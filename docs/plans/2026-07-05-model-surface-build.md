# Plan — Model surface: catalog + Quick Setup + embeddings (the "rest of the model work")

> ⛔ **LIVE STATUS (2026-07-05): APPROVED (user "do the plan fold it in and code" → ExitPlanMode approved). IN BUILD, phase-by-phase.** This is the executable task plan + live tracker. ONE plan folding: the approved **model-surface redesign** (`2026-07-03-model-setup-simplification.md` §1–15, LLM side APPROVED + panel-validated) + the now-unblocked **embedding half** + the A/B/C follow-ups from the SVM plan + this session's corrections. Built on `claude/admiring-galileo-il3q0o`; each phase pre-build rules-checked, ships green (ruff+pytest · build:vite+headless smoke 0 JS errors · JV import+grep · live curl · diff rules-checker), then **commit + push immediately** (the container was unstable — reset 3× during planning; small pushed commits are the only reset-proof unit).

## Context (why)
On testing the model surface the user found it confusing (`2026-07-03` §12): the catalog list, the "Load" button implying "the model the app uses" (it only trials a model), the embedding effectively pinned to nomic with no picker, several wrong dense fit numbers, and the card options stopping at 24 GB. A full redesign was discussed + **approved** (§1–15). Its embedding half was **blocked** behind the "embedding-serving gap" (§12: can the bundled runner hold an embed resident alongside chat?) — **P3 of the SVM plan shipped co-resident embeddings**, clearing it. This plan executes the redesign, folding this session's decisions.

## Decisions folded (LOCKED — do not re-open without the user)
- **Llama-3.3-70B, NOT 3.1.** The user-APPROVED dense ladder (§14(1)/§15) is Qwen3-8B / 14B / 32B / **Llama-3.3-70B**. A stray `llama-3.1-70b` from the Gemini box ladder must NOT be seeded — 3.3 is the correct, newer, better model (agent-error correction, recorded).
- **Catalog actions = Download + Set-as-default / Set-as-embedding. NO Load/Unload/Retry** (design §14; supersedes the SVM "4b" per-model residency controls the user dropped: *"4b use Download/Set-as-default surface, leave"*).
- **Multi-residence = chat + embed only.** Quick Setup auto-picks ONE **chat** model; the tiny **embed** co-resides (`models_max` default 2). Several big chat models is only the optional `models_max` extra, never the default flow (user confirmed). The design's "one model" premise holds for the chat model.
- **Embeddings ladder (§2/§14(2), now unblocked):** keep **nomic-embed-text** (CPU floor) + ADD **qwen3-embedding-0.6b** (Q8_0), **bge-m3** (multilingual, MIT), **Qwen3-Embedding-8B** (#1 multilingual MTEB — the "high" embed the user asked for). Web-verify every HF repo (upstream-audit rule).
- **`quality_rank` KEPT** (exists at `db.py:111`; §15 reversed the earlier "drop"). Add an explicit editable **`embedding` boolean** column (does NOT exist today — identity is only the `/embed/i` name regex in `ProviderForm.vue:75` + a provider's `embedding_model` string; both stay, but the catalog gets a real flag).
- **Auto-pick = the §10 speed-floor rule** (dense-fully-on-GPU OR usable A3B-MoE-offload; EXCLUDE slow dense-partial-offload; rank survivors by `quality_rank`; fall back to best-runnable).
- **Other-provider Quick Setup (NEW):** wire the ALREADY-BUILT but UNWIRED `GET /v1/llm-providers/detect-local` (Ollama :11434 + LM Studio :1234) + the ProviderForm preset chips into a Quick Setup provider step, so setup isn't bundled-runner-only.
- **User reservation (recorded):** the user *"doesn't really like us just seeding models."* Keep the seed a SMALL curated ladder, lean on the **Smart Add flow** + the editable catalog, and record a flagged FUTURE — *fetch the curated catalog from a remote so new models don't need an app release* (NOT built now).

## Current-state corrections (verified via Explore 2026-07-05 — the design docs were stale on these)
- **CORRECTION (verified 2026-07-05 during Phase 2 — this note had wrong details): `LuModelCatalog.vue`** did have an installed-first framing with a **`browseOpen` "Browse catalog" toggle** (`:202`) + `yourModels`/`availableModels`/`shownModels` — NOT a "plain always-visible table"; the "no search/sort" half WAS correct (Phase 2 adds them). The stale `:394-409` action-cite + `:95-98` join-cite were impossible (the file was 381 lines; actions were `:242-260`, the license join `:64-68`). Actions before Phase 2: Edit / Delete / Tune + **Load / Unload / Download&load / Retry** via `load()`→`POST /v1/llm-runner/load` + `unload()`→`POST /v1/llm-runner/stop`; **no Set-default/Set-embedding.** Reads `GET /v1/llm-runner/models` (fit list) + `/status` + `/v1/ai/model-catalog` (license). **Phase 2 REPLACED all of that** (see LIVE PROGRESS).
- **CORRECTION (verified 2026-07-05 during Phase 1 — this note was wrong): `useCatalogMeta.js` DOES EXIST** at `ui/src/common/composables/useCatalogMeta.js` (the shared singleton fetching `/v1/ai/model-catalog`, exposing `qualityById`/`licenseById`/`useLimitedById`/`descriptionById`/`poolingById`, consumed by both QuickSetup + LuModelCatalog). Phase 1 ADDED `typeById` + `embeddingById` to it. Only **`modelApply.js` does not exist** (Phase 2 builds it). The set-default/set-embedding writers exist, unused, in `ui/src/composables/useRouting.js` (`setDefaultLlm` `:38`, `setDefaultEmbedding` `:43`, `saveRouting`→`PUT /v1/ai/routing`) — Phase 2 builds `modelApply.js` on those.
- **Badge join wrinkle:** routing default is `{providerId, model}`; catalog rows key on catalog `id`. The Default/Embedding badge needs an explicit join (nothing joins them today).
- **`QuickSetup.vue`:** `CARD_OPTIONS` stops at 24 GB (`:57-64`); the **embedding line is DISPLAY-ONLY** (`:322` "Embedding stays as set in Providers") — no editable embed row. Apply writes `PUT /v1/ai/routing` (default+jobs+pins) then loads the default; ranks by params / `recommendations[].rank`. It **cannot see `type`** (dense|moe) — that's only on `CatalogRow` (`model_catalog_api.py:42`), not on `/v1/llm-runner/models` (`RunnerModelInfo` has `fit`+`active_params` but no `type`). **The `?vramMb` "bug" is a non-issue** — QuickSetup already sends `?vram_mb=` (`:107`) matching the FastAPI arg (`api.py:78`); do NOT "fix" it.
- **Download resolver** `models.py:select_files` (`:84-115`) substring-matches `quant` against `.gguf` paths (so `Q8_0` → `*-Q8_0.gguf` + shards). Good — no filename hardcoding.

## Phases (each: pre-build checker → build → verify green → diff checker → commit+push)
- **Phase 0 — Seed reconciliation + fit fixes + `embedding` flag (backend, runner).** In `seed.py DEFAULT_CATALOG`: keep qwen3-14b / qwen3.6-35b-a3b / glm-4.5-air / nomic; ADD Qwen3-8B, Qwen3-32B, **Llama-3.3-70B** (dense) + embeds qwen3-embedding-0.6b / bge-m3 / Qwen3-Embedding-8B; DROP qwen3.5-9b?/gemma-4-12b/mistral-24b/qwen3.6-27b/gemma-4-31b/llama-4-scout/qwen3-235b per §14(3) (confirm the exact keep/cut set against §2 at build). Correct the §4-audited fit numbers. Add `ModelCatalog.embedding` bool (`db.py`) + `CatalogRow.embedding` (`model_catalog_api.py`) + thread through `stores.py`/`seed.py`. Reuse the existing `quality_rank`/`pooling` columns. `seed_default_routing` → default embed = qwen3-embedding-0.6b. Drop+reseed (pre-prod, no migration). Web-verify every HF repo.
- **Phase 1 — Auto-pick speed-floor rule (backend/shared).** Give the picker `type`: add `type` to `RunnerModelInfo`/`get_models` (`api.py:113-126`, from the catalog) OR fetch `/v1/ai/model-catalog` in the picker. Implement §10 (dense fit==ok OR moe fit∈{ok,tight}; exclude dense+tight; rank by `quality_rank`; fallback).
- **Phase 2 — Catalog UI redesign (shared kit) + `modelApply.js`.** `LuModelCatalog.vue`: add search + sort + fit-grouping to the table; replace Load/Unload/Download&load/Retry with **Download** + **Set-as-default** (LLM) / **Set-as-embedding** (embed rows, gated on the new flag); badges Not-downloaded/Downloaded/Default/Embedding (join routing↔catalog id). New `ui/src/common/services/modelApply.js` wrapping `useRouting` writers (set-default/set-embedding + current-default/embedding for badges), consumed by the catalog AND Quick Setup. Keep Edit/Delete/Tune. `LuModelPicker.vue` (per-task override) UNCHANGED. **DOC (flagged by the Phase-1 diff-checker):** rewrite `justwrite-app/docs/models.md` §"The model catalog" (`:37-53`) here — it still describes the removed "installed-first / Browse catalog toggle" + the "Load / Download & load" actions this phase replaces; left as-is in Phase 1 because that UI still ships TODAY (rewriting it in Phase 1 would document non-existent UI).
- **Phase 3 — Quick Setup redesign (shared kit).** `CARD_OPTIONS` → add 32/48/64 GB; make the embedding line an **editable dropdown** (fitting embeds via the new flag, best-fit default) not display-only; auto-pick via the Phase-1 speed-floor rule; add the **other-provider step** (wire `detect-local` + preset chips → create an Ollama/cloud provider). Reuse `modelApply.js`.
- **Phase 4 — Smart Add flow (shared kit).** Build the paste-repo → Get-info (quant dropdown + per-quant size) → Test-fit → auto-description → Download flow on the shipped `POST /v1/ai/model-catalog/inspect` endpoint.
- **Phase 5 — 4a knobs before install (small).** Show the two residency knobs in `LuRunnerEngine.vue` even when the engine isn't installed (the user's "C").
- **Phase 6 — Docs + ship (continuous per phase).** `ai-providers.md` / `models.md`, `MORNING_RECAP.md`, this plan's live status; record the 3.1→3.3 correction + the seed-reservation future note.

## Out of scope (separate work)
- **JV shared-LLM convergence** (its own future plan). **#28 measured benchmarks** (box-only) + **#25 per-job recs** (needs #28). The remote-fetched-catalog future (recorded, not built).

## Verification (per phase)
runner `ruff`+`pytest` · JW `build:vite` + `node scripts/headless-smoke.mjs` (0 JS errors) + a Playwright probe for the new surface · JV `import llm_runner.llm.*` + grep-for-removed-symbols (JV renders none of these; NOT a JV boot — known-red) · live `POST /v1/data/reset` + curl the touched endpoints · pre-build + diff rules-checker · **commit + push per phase** (reset-proofing). No PR unless asked.

## LIVE PROGRESS
- **Phase 0 — DONE + VERIFIED (2026-07-05).** Added the `ModelCatalog.embedding` boolean (`db.py`) + `CatalogRow.embedding` (`model_catalog_api.py`) threaded through `stores.py` (`_catalog_to_wire` + upsert) + `seed.py` `seed_default_catalog`. Reconciled `DEFAULT_CATALOG` 16→**10** rows (per-row strict-diff): KEPT qwen3-14b · qwen3-32b · qwen3.6-35b-a3b · glm-4.5-air · nomic · qwen3-embedding-0.6b; ADDED qwen3-8b · **llama-3.3-70b** (NOT 3.1 — corrected #120's error) · bge-m3 (pooling=cls) · qwen3-embedding-8b; DROPPED the 10 others incl. the two `llama-3.1-70b` rows + gemma/mistral/scout/235b. All 4 embeds flagged `embedding=True` (nomic=mean · qwen3-0.6b=last · bge-m3=cls · qwen3-8b=last). **Every repo/quant/license web-verified via the HF API 2026-07-05** (Qwen3-8B/32B apache · Llama-3.3-70B llama3.3→use-limited · Qwen3-Embedding-0.6B/8B apache · bge-m3 MIT). Tests: repointed `test_identity.py` (qwen3.5-9b→qwen3-14b) + `test_switch_resolve.py` (dropped-id lookups → kept models; reframed the dense-mtp test onto the surviving 35b-a3b-mtp since no dense+mtp survives) + a new `test_embedding_flag_seeded_on_embeds_not_llms`. Docs: `justwrite-app/docs/models.md` (curated-ladder + explicit chat/embed flag). VERIFIED: ruff clean + **298 pytest** + a live seed→store→wire check (10 rows, correct embedding flags + pooling + use-limited). Pre-build rules-checker FAIL(5)→all folded (the 16-row re-ground, the 3 missed drops incl. the 3.1 rows, the test fixes, docs-with-Phase-0, bge-m3 pooling=cls). Diff rules-checker + commit this turn.
- **Phase 1 — DONE + VERIFIED (2026-07-05).** Implemented the §10 speed-floor auto-pick ("the most capable model that still streams faster than you read") in the shared kit — NO backend change (the picker gets `type` via the catalog join, per design §10 line 63 + §13 line 69, NOT by widening the shared `RunnerModelInfo`, which JV also consumes via the mounted runner router — the reuse-correct + JV-safe path the design resolved the build plan's "add-type-to-RunnerModelInfo OR fetch-catalog" OR to). FILES: (1) NEW `ui/src/common/services/modelPick.js` — the PURE `pickBestModel(models, {typeOf, qualityOf, isEmbed, isUseLimited})` + the exported `FIT_RUNNABLE`/`FIT_RANK` (ONE source of the runnable-set + fit-rank, so "runnable" can't drift between the module and QuickSetup). The rule VERBATIM: candidates = runnable (`fit ∈ {ok,tight,cpu}`) AND NOT embedding AND NOT use-limited; FAST-ENOUGH = (`type==dense` AND `fit==ok`) OR (`type==moe` AND `fit ∈ {ok,tight}`), EXCLUDING dense+tight (the slow partial-offload); if FAST-ENOUGH non-empty → lowest `quality_rank` among it (tie-break: better fit via `FIT_RANK`); else FALLBACK → lowest `quality_rank` among ALL runnable; "" if none runs. (2) `ui/src/common/composables/useCatalogMeta.js` — ADDED `typeById` (dense|moe) + `embeddingById` maps (pattern-matched the existing by-id maps; both on the wire — `stores.py:289` `type=r.type`, `stores.py:292` `embedding=r.embedding`). (3) `ui/src/views/QuickSetup.vue` — imports `pickBestModel`/`FIT_RUNNABLE` from modelPick.js (removed the LOCAL `FIT_RUNNABLE`+`FIT_RANK` redefinitions — the checker's T3 single-source fix); `bestFittingId()` is now a thin wrapper binding the catalog-join accessors; header + inline comments rewritten to the speed-floor rule. (4) NEW `scripts/verify-model-pick.mjs` — the committed 14-case Node truth-table (the ONLY way to verify §10's crux — a dense+tight EXCLUDED while a moe+tight is KEPT — which no browser render/card-override probe can isolate, since the 32/48/64 GB cards are Phase 3). (5) `justwrite-app/docs/models.md` §Quick Setup rewritten to the speed-floor wording (the pre-build checker's one FAIL, T11: the old "highest-quality that fits / CPU-spill still counts" text became FALSE under the rule; folded into this same change, not deferred to Phase 6). **THE bge-m3 LEAK FIX (correctness, found during grounding):** the exclusion of embeddings now uses the explicit `embedding` FLAG (the OR-ed regex is a fallback), because bge-m3 has NO "embed" in its id/name — the old `/embed/i` regex alone would have leaked bge-m3 (a Phase-0 embed, fits `ok` everywhere at 568M) into the LLM candidate pool and let Quick Setup pick an embedding as the chat default (proven on a low-VRAM box where only bge-m3 fits). This is EXACTLY why Phase 0 added the flag (`model_catalog_api.py:50` "replaces the /embed/i guess") — using it here is following the design, not scope-creep. **use-limited-KEEP** is a deliberate augmentation of §10's candidate set (documented in modelPick.js + QuickSetup): a use-limited license (e.g. Llama-Community → Llama-3.3-70B) is "never an auto-default" (`seed.py:103-104` + the Llama row) — removing the guard would silently auto-set Llama-3.3-70B on a ≥48 GB box; the checker confirmed this is CORRECT, not scope-creep. **VERIFIED:** §10 truth-table **14/14** (crux, embed-exclusion, use-limited, lowest-quality-wins, tie-break-better-fit, dense+cpu→fallback, empty→"") · runner ruff clean + **298 pytest** (no py change → no regression) · JW `build:vite` compiles the kit change · JW headless smoke **ALL routes, 0 JS errors** (Providers & models + provider-form clean — a stale-DB `trained_ctx` drift on two unrelated AI sub-tabs was fixed by a `POST /v1/data/reset`, NOT my change) · LIVE `/v1/ai/model-catalog` carries `type`+`embedding` on all 10 rows + the §10 pick @16 GB resolves to **qwen3-14b** (both 8B/14B dense+ok fast-enough → lower quality_rank 25 wins) with bge-m3 correctly excluded by the flag. **Pre-build rules-checker: FAIL(1 · T11 models.md)→folded; the structural question (extract modelPick.js vs inline) was adjudicated JUSTIFIED (the only path to verify §10's crux); T3/T5 strengtheners folded (single-source FIT_*, +tie-break/+dense+cpu/+lowest-quality truth-table cases). §13:69 "drop qualityById" is STALE (pre-2026-07-04) — correctly ignored (quality_rank is KEPT per §15).** Diff rules-checker + commit this turn.
- **Phase 2 — DONE + VERIFIED (2026-07-05).** Catalog UI redesign + the shared `modelApply.js`, built in two reset-proof sub-commits.
  - **2a (shared service + QuickSetup refactor):** NEW `ui/src/common/services/modelApply.js` — a module-singleton (the useRunnerModels/useCatalogMeta precedent) holding the applied-state badge refs (`currentDefaultId`/`currentEmbeddingId`) + `setAsDefault(modelId)` (the non-clobber preset-write, extracted VERBATIM from QuickSetup.apply — PUTs `{...p, model}` so every task preset keeps its per-task settings) + `setAsEmbedding(providerId, modelId)` (REUSES `useRouting.setDefaultEmbedding` — NO second routing PUT) + `refreshApplied` + the `dominantOf` helper. PROMOTED `useRouting.js` from `ui/src/composables/` → `ui/src/common/composables/` (it had ZERO consumers, so common/ can build on it without importing "up") + made its setters `return saveRouting()` (awaitable, so setAsEmbedding persists before the badge refresh). `QuickSetup.apply()` refactored to `await setAsDefault(target)` + `await setAsEmbedding(pick.embeddingId, pick.embeddingModel)` (behavior-preserving — the embedding keeps the user's SAVED provider, not a hardcoded local one). Converged the duplicated `FIT_LABEL` (was in useRunnerModels + QuickSetup) into `modelPick.js` beside FIT_RUNNABLE/FIT_RANK.
  - **2b (`LuModelCatalog.vue`):** the installed-first `browseOpen`/Your-models/Browse-catalog framing REPLACED with ONE visible **fit-grouped** list (fitting first → a divider row → non-fitting) + a **search** box + a **sort** control (quality|name|size). Load/Unload/Retry/Download-anyway REMOVED → **Download** (not-downloaded rows) + **Set as default** (downloaded LLM, disabled when already default) / **Set as embedding** (downloaded embed rows, gated on the `embedding` flag). NEW **Default** / **Embedding** badges (UiTag, id-join on the applied refs; Embedding badge additionally gated on `routing.default.embeddingId===local-llamacpp`). Edit form gained an **Embedding model** checkbox (+ `embedding:false` in `blankModel`). Footer copy corrected ("models load automatically" — the router co-resides). Dead `unloadModel` + the `load`/`unload` destructure removed.
  - **Pre-build 3-checker PANEL (architecture-fit · reuse · grounding) — all validated the core shape; FAILs were convergent + folded:** (T3, UNANIMOUS) setAsEmbedding must REUSE useRouting (promoted to common/) not a second writer; (T1, arch) setAsEmbedding signature `(providerId, modelId)` so QuickSetup's saved embedding provider is never silently clobbered to local; (grounding) setAsDefault must PUT `{...p, model}` to preserve all 15 EnginePresetRow fields; import FIT_RUNNABLE from modelPick (not redefine); gate the Embedding badge on the local runner; keep Download for `fit==no` rows.
  - **VERIFIED:** build:vite compiles (2a+2b) · headless smoke **ALL routes + provider-form (search=true, catalog, add-modal) 0 JS errors** (the JW smoke's `provider-form` probe was updated browse→search + to assert the real toolbar controls, since the Browse toggle is gone) · **LIVE round-trip:** `setAsDefault` flips p_chat `qwen3.6-35b-a3b-mtp`→`qwen3-14b-q4_k_m` with ALL per-task fields (samplers/temperature/jsonMode/maxTokens) **PRESERVED**; `setAsEmbedding` sets `bge-m3` keeping `llmId`+pins · runner ruff + **298 pytest** (no py change) · **JV safe** (own local QuickSetup; no refs to useRouting/modelApply/LuModelCatalog). `justwrite-app/docs/models.md` §"The model catalog" rewritten to the new UI (the Phase-1-flagged stale doc, now current). Diff rules-checker + commit this turn.
- **Phase 3 — IN PROGRESS (2026-07-05).** Quick Setup redesign. USER chose **Option 2** for the other-provider path (connect a provider AND set its model as the default; provider-aware).
  - **3a — DONE + VERIFIED:** `CARD_OPTIONS` +32/48/64 GB; the embedding line is now an **editable `UiSelect`** of the FITTING embed models (best-fit default via the §10 embedding pick); extracted a shared **`pickLowestQuality(models,{qualityOf})`** comparator from `modelPick.js` (the reuse lens's T3 — `pickBestModel` + `bestEmbedId` both use it; truth-table still **15/15**). VERIFIED: build:vite + headless smoke 0 JS errors + a live wizard probe (embed = dropdown not a div; cards 32/48/64 present; 0 JS errors bar the benign blocked Google-Font resource).
  - **3b-i — DONE + VERIFIED:** the REUSE foundation — NEW shared `ui/src/common/composables/useProviderConnect.js` (`PROVIDER_PRESETS` + `detectLocal`/`probeModels`/`createProvider`) + **ProviderForm refactored onto it** (its local PRESETS literal deleted; its inline probe-models + create POSTs → the composable; `request` kept for the PATCH/DELETE). Behavior-preserving — build:vite + headless smoke 0 JS errors; diff-checker PASS (T3 single-source confirmed, T5 identical bodies).
  - **3b-ii-a — DONE + VERIFIED:** the provider-aware contract — `modelApply.setAsDefault(providerId, modelId)` writes `{...p, providerId, model}`; `dominantOf` now returns the dominant preset's `providerId`; `refreshApplied` gates `currentDefaultId` on `providerId===local-llamacpp` (an external default won't false-badge a same-id local catalog row — mirrors the embedding gate). Both existing call sites updated to pass `LOCAL_RUNNER_ID` (catalog `makeDefault`, QuickSetup local apply) = a NO-OP on providerId (presets already point at the bundled runner) → behavior-preserving. VERIFIED: build:vite + headless smoke 0 JS errors + a live round-trip (a preset flips `(local-llamacpp, qwen3.6-35b-a3b-mtp)` → `(openai-compat-local, llama3.1:8b)` with all per-task fields PRESERVED).
  - **3b-ii-b — DONE + VERIFIED (2026-07-05; ONE kit file `ui/src/views/QuickSetup.vue` + a `listModels` helper on `useProviderConnect.js`; NO backend change — every endpoint already shipped).** A **"Run models with" `UiSelect`** = *Bundled runner (recommended)* + the REACHABLE providers (`registered && (local || hasApiKey)`, excluding `local-llamacpp`) — all three fields verified on `LLMProviderResponse` (`provider_api.py:52-62`). Bundled → the current confirm flow unchanged. An external provider → a **Model `UiSelect`** populated from `GET /v1/llm-providers/{id}/models` (the registered adapter's STORED key). A **"+ Connect a provider"** section: `detectLocal()` on open → one-click **Connect** for detected-but-unregistered Ollama/LM Studio (local, no key); the cloud **preset chips** (`PROVIDER_PRESETS` where `isLocal===false`, rendered with the kit **`UiChip`**, NOT `.lu-pf-chip`) → a slim inline API-key `UiInput` + Connect; both create via `useProviderConnect.createProvider`, then select the new provider + list its models. `apply()` branches: bundled = `setAsDefault(LOCAL_RUNNER_ID, target)` + embed + download/load; external = `setAsDefault(providerId, providerModel)` + embed, **SKIP** the runner load (the provider serves itself). **DEVIATION from the old line-52 spec (adjudicated CORRECT by the pre-build checker):** the external model list uses **`GET /v1/llm-providers/{id}/models`** (a NEW `listModels(id)` on `useProviderConnect` — the T3 single-source fix), NOT `probeModels`: `probe-models` (`api.py:87-108`) is a pre-save DRAFT probe needing a client-supplied key, and a saved cloud provider's key is write-only (`provider_api.py:78-80`), so the client can't re-supply it; `/{id}/models` (`api.py:67-76`) uses the registered adapter's stored key and works uniformly for pre-existing AND just-connected providers. **PRE-BUILD rules-checker FAIL(1 · T5) → all folded** (the external path was input-only): (1) SURFACE `/models` `{error}` (`providerModelsError`) + `createProvider` rejections (`connectError`) — never a silent blank dropdown (an empty list with no error shows "No models found for this provider"); (2) BRANCH the confirm-preview / apply-progress / done-summary copy for external ("Run models with {provider}·{model}", drop the download line) — those outcome blocks were bundled-only + false for an external default; (3) RESET `runWith`/`providerModel`/connect-state in `openWizard` so a reopen after an external Apply isn't stranded. Minors folded: `loadProviders` once in `openWizard` (not `loadAll` → no refetch on a card change); guard `setAsEmbedding` on a chosen embed. **VERIFIED:** build:vite compiles · full headless smoke ALL routes + AI sub-tabs **0 JS errors** · a NEW committed live wizard probe `justwrite-app/scripts/qs-otherprovider-probe.mjs` (**12/12, 0 page errors**: reachable filter shows the keyed cloud + hides the keyless + excludes bundled · bundled↔external branch swap · "nothing downloads" copy · connect chips + key-input reveal) · LIVE API round-trips: reachable-filter data (keyed `registered+hasApiKey+!local` reachable vs keyless `!hasApiKey+!local` hidden) · external `setAsDefault` PUT flips a preset `(local-llamacpp, qwen3.6-35b-a3b-mtp)` → `(test-openai, gpt-4o)` with ALL per-task fields PRESERVED (zero drift) · `/{id}/models` returns error-as-data (an unreachable openai-compat lists `[]` gracefully → the UI shows "No models found", never a blank). **JV safe** — a fresh grep of `/home/user/JustVoice/src` = ZERO references to `QuickSetup`/`useProviderConnect`/`listModels`/`setAsDefault`/`modelApply`. **Phase 3 COMPLETE.** Diff rules-checker **VERDICT PASS** (all T1–T12 PASS/NA); commit (runner + JW) this turn; `models.md` §Quick Setup + recap updated in the same series. **Flagged non-blocking follow-ups (diff-checker):** (T3 convergence) a future `listProviders()`/`listModels` pass — `loadProviders`' raw `GET /v1/llm-providers` is the 5th inline of the list-all (also `useRouting.js`/`TaskKinds.vue`/`AiModelsArea.vue`/`FeatureWorkbench.vue`) + `LuModelPicker.vue:51` still inlines `/{id}/models` (both OUT of 3b-ii-b scope; no forked logic, so not a blocker); (T7 e2e) the committed probe drives the external branch with SEEDED unreachable providers (proves the error + "No models found" paths), so a POPULATED external dropdown from a live reachable provider is verified piecewise (the `/{id}/models` endpoint returns real models — proven via a keyless anthropic returning 6; `providerModelOptions` is a trivial map; the SAME `UiSelect` renders the bundled model list) but not end-to-end in one probe.
  - **NEXT (Phase 4, separate work): the model-catalog + Tune "providers" body of work** the user raised 2026-07-05 (catalog grid Params→Type column; type/mtp/embedding CHECKBOXES above the fit estimate in the edit form; quant DROPDOWN + Q/IQ label — already Phase-4 Smart-Add scope; the MTP separate-draft-file `--model-draft` gap + `reasoning-budget` switches; storage-card wording; the shared `LogsPanel` clear/per-day/delete-all). And **the per-model Tune SAVE design** (user recommendation, PARKED to discuss AFTER 3b: KEEP it OFF the `modelApply` kit surface — it is a switch-layer concern, not a which-model concern; switches today layer base→type→hardware in `switch_presets` with NO per-model layer, design §6.5 — a per-model save is a design-doc decision to settle with the user first). These are a NEW plan, sequenced after 3b-ii-b per the user.
  - **Pre-build 3-checker PANEL done — all validated the DIRECTION; convergent FAILs folded:** (reuse, all 3) extract `useProviderConnect` + share the quality comparator + kit chip not `.lu-pf-chip`; (grounding) the preset→provider routing mechanism is **`prompts.py:458/499` (`provider_override=preset.providerId`) → `dispatch.py:166-184`**, NOT `dispatch.py:59` — the citation was wrong but the CONCLUSION holds (writing providerId onto a preset routes to that provider); (arch) DECIDED the connect-shape = inline via `useProviderConnect` (merits: user chose the complete front door; the composable removes the duplication that would justify delegating to ProviderForm), the "Run models with" list must show **reachable** providers (local-detected OR cloud-with-key) NOT bare registered, and `dominantOf` must also return the dominant preset's `providerId` to gate the Default badge on local; (T11, all 3) docs land per sub-commit; **JV safe** — verified ZERO consumers of `modelApply`/`setAsDefault` in the JV tree (it mounts its own local QuickSetup). Embedding runtime caveat: the dropdown is UI-only; whether `/v1/ai/embeddings` on `local-llamacpp` serves the co-resident embed is a box-only check (flagged, not claimed).

- **Phase 4 (Smart Add) — MOSTLY SUBSUMED by the catalog+tune PLAN B build (2026-07-05, see `2026-07-05-catalog-tune-providers-phase.md`); ONE remainder.** What Plan B shipped covers Phase 4's substance: paste-repo → **"Read from link"** now does ONE HF tree call (`POST /v1/ai/model-catalog/list-files`) → the **quant DROPDOWN with real per-quant sizes** (+ QAT/IQ labels + a Custom free-type escape + a fits-your-box pre-pick) → the header inspect fills the facts + the real download size + the VRAM estimate (the "Test fit" substance — shown in the Auto-detected block) → Download. Plus beyond Phase 4's scope: MTP-draft auto-detect + pre-select. **REMAINDER (not built): the auto-composed factual description** ("distilled from the file facts" — today `description` stays hand-typed curation). Small, standalone; needs a user "go".
- **Phase 5 (4a knobs before install) — NOT BUILT (verified 2026-07-05: `LuRunnerEngine.vue:173` gates the resident-set + `models_max`/`sleep_idle_seconds` knobs behind `v-if="installed"`).** The user's "C" wanted the two knobs visible/editable BEFORE the engine is installed. Small, standalone; needs a user "go".
- **Phase 6 (docs, continuous) — DONE CONTINUOUSLY** — every shipped phase landed `models.md`/`ai-providers.md`/recap/this tracker in the same change (checker-verified per phase); the 3.1→3.3 correction is recorded in the LOCKED DECISIONS (recap) + Phase 0 entry.
