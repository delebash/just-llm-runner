# Plan — GGUF-grounded model layer (JustWrite AI)

> **⛔ STATUS: APPROVED 2026-07-02 (user). Phases 1–6 SHIPPED 2026-07-03 (Phase 5: runner `50b7a04`, JW `ee92054`; Phase 6 docs: runner `67d7a61`, JW `843064f`). ✅ FEATURE COMPLETE.** Persisted from plan mode + a 3-checker rules panel; built phase-by-phase on `claude/admiring-galileo-il3q0o`. Scope: shared `just-llm-runner` (runner + `ui` kit), consumed by JustWrite. **JustVoice is out** (build/verify JW only; JV inherits the shared changes). No PR unless asked. Items **1–5**; item 6 (QuickSetup `/v1/ai/jobs`→taskKind, #100) deferred.
>
> **⚠️ SCHEMA NOTE (drop+reseed, no migration).** Phase 2 added the `trained_ctx` column on `model_catalog` + the new `model_samplers` table. `db.create_all` creates the MISSING `model_samplers` table, but does NOT `ALTER` the existing `model_catalog` to add `trained_ctx` — so an existing DB MUST be dropped + reseeded (the plan's chosen no-migration path) or `set_derived`/`upsert` writes to `trained_ctx` will error. Fresh installs + the in-memory test DB are unaffected.
>
> **Validated by a 3-checker rules panel (architecture-fit · reuse/convergence · grounding).** All three FAILs are folded below and marked 【panel】: the HF-vs-range-read grounding fix (T2), the coarse-map + hardware-band definition (T1), the `tasks[0]` mount/providerId fix (T1), the orphaned-mtp-preset removal (T3), docstrings-in-their-phase (T11), the stale `:447` copy (T5), and the `embed` data source (T5).

## Context — why this change
Walking the model/tune surface, the user hit a chain of problems that all trace to **one root cause: model facts are hand-typed, not read from the file.**
- `mtp` is a hand-typed seed flag (`seed.py:116` `"mtp": True`) — nothing detects it, even though the GGUF **binary header** exposes it (`{arch}.nextn_predict_layers`). Our `identity.py:8-10` comment + the UI copy `LuModelCatalog.vue:447` both wrongly claim it's undetectable.
- Fit + params + quant + context are all hand-typed (`seed.py:97-140`). But **the GGUF header is readable from the HF link with zero weight download** — we already read it locally (`gguf.py:96-98`) and drop most of it. (Live-checked this session: the HF **API** exposes `context_length`/`architecture`/**real file size** but NOT the per-arch hyperparameters; `mtp`/`type` come from a **range-read of the binary header** — see Phase 1.)
- The **MoE+MTP resolve rule** (`switch_resolve.py:53`) blanket-skips MTP for every MoE model. This was an **intentional §6.5 choice** (`switch_resolve.py:9-12`) that our **own later research superseded** (`2026-06-27-model-catalog-research-and-recommendations.md:89`: "MTP is machine-dependent — measure, don't dogmatize"). It's now wrong for the flagship MoE+MTP case — proven in-repo by our own `qwen3.6-35b-a3b-mtp` (`seed.py:121-124`, `mtp:True, type:moe`), whose MTP is silently disabled.
- The **catalog↔recommendations** surface is two hand-curated places that overlap; the recommendation picks (chat=small, the per-tier matrix) were a reviewer-panel + user judgment, **never measured** (#28 open).
- **Tune & measure is a dead end** — you tune switches, measure, then must re-type them in the Lab to keep them.

**Outcome:** the model *file* becomes the source of truth (read from the link, pre-download), fit is *live* everywhere (nothing estimated), MTP is detected + honestly measurable, the recommendation surface becomes a **per-hardware grid** computed from real fit, and Tune & measure hands its tuned switches straight into a task's Lab column.

## ⛔ LIVE STATUS — where this stands (kept current)
Branch `claude/admiring-galileo-il3q0o`; JW-only; verify per phase (runner ruff+pytest · build:vite · headless-smoke 0 JS errors · Playwright probe · rules-checker). Plan panel-reviewed (3 checkers) + folded. **APPROVED — Phases 1–6 SHIPPED (2026-07-03). ✅ FEATURE COMPLETE.**
- **Phase 1 — GGUF metadata from the link:** ✅ **DONE (2026-07-03).** Key names verified against 2 real headers (Qwen3.6-27B `qwen35` dense+MTP; GLM-4.5-Air `glm4moe` MoE+MTP). `gguf.py` extended — `GgufMeta` gains `context_length` · `nextn_predict_layers` (→ `is_mtp`) · `expert_used_count` · `file_type` · `sampling` dict (`general.sampling.*`) · `base_repo_url` (`general.base_model.0.repo_url`, for the generation_config fallback); array values now SKIPPED (seek-past `_skip_array`, never materialised); parser split into `read_gguf_metadata_from_stream(BinaryIO)` so a local file AND a remote `BytesIO` share ONE parser. New `gguf_remote.py::fetch_gguf_meta(repo, quant)` → range-reads the header (reuses `models.select_files`; bounded 24 MB read + one 4× retry on a truncated header) → `(GgufMeta, total_weight_bytes)`, **no new dependency**. **Verified END-TO-END pre-download** on the 17 GB Qwen3.6-27B (ships `sampling={temp:1.0, top_k:20, top_p:0.95}`, mtp) + the 68 GB GLM-4.5-Air (moe 128/8, mtp, `sampling={}`→fallback via `base_repo=zai-org/GLM-4.5-Air`) — zero weight bytes. 4 new tests (`test_gguf.py`); runner **206 pytest + ruff green + rules-checker PASS (all T1–T12)**. `fit.py` UNCHANGED — its precise estimate already takes the real inputs (`size_mb`/`n_layers`/`n_kv_heads`/`embedding_dim`/`ctx_size`); the pre-download call-site feed lands in Phase 2 (the `/inspect` endpoint) so no dead code is added now.
- **Phase 2 — Auto-derive catalog fields (file = source of truth):** ✅ **DONE (2026-07-03).** The GGUF file now grounds the catalog's capability facts. `db.py` gains a `trained_ctx` column on `ModelCatalog` + a new `model_samplers` child table (model_id, param_name, value; no FK, mirroring `ModelRecommendation` / `engine_preset_samplers` / `feature_sampler_params` — a relational child, NOT a JSON blob). `identity.py`: the stale "mtp is NOT GGUF-detectable" docstring is corrected (mtp IS inferred now from `nextn_predict_layers`); a new `derived_fields_from_meta(meta)` maps ONE header read → `{type, mtp, trained_ctx, total_params, size_label, samplers}`; `detect_and_store_model_type` (post-download) writes type/mtp/trained_ctx/total_params + the sampler rows via a new `ModelCatalogStore.set_derived` (preserves `built_in`), with an optional `samplers_fallback`; a new `inspect_model_from_link(repo, quant)` reads the header PRE-download (range-read, no weights) and returns the same facts + the real download size + a VRAM estimate. `gguf.py` reads `general.size_label` — VERIFIED against the GGUF spec (docs/gguf.md) AND 2 real headers 2026-07-03: dense `general.size_label='27B'` (the param count) → file-derived into `total_params`; MoE `general.size_label='128x9.4B'` (expert-config) does NOT decompose → MoE params stay curation. `gguf_remote.py` gains `fetch_generation_config_samplers(base_repo_url)` — the header→generation_config.json→generic sampler precedence (HF key names → llama.cpp names, best-effort {} on error). `model_catalog_api.py`: `CatalogRow` gains `trainedCtx`+`samplers`; a new `InspectResponse`; a new `POST /v1/ai/model-catalog/inspect` mounted via an optional `inspect_fn` callable (mirrors the existing optional `resolve_switches` seam). `install.py` wires `inspect_fn` + the post-download `samplers_fallback`. `LuModelCatalog.vue`: the Edit form's hand-editable Type select + MTP checkbox are REPLACED by a read-only "Auto-detected from the file" section (type/mtp/trained-ctx/recommended-samplers/size-label + download-size/est-VRAM after inspect); a "Read from link" button calls `/inspect`; the stale ":447 not GGUF-detectable" copy is gone; `UiSelect`/`TYPES` removed (unused). **Verified: runner 220 pytest + ruff green (14 new tests incl. the carry-forward edge tests); JW build:vite clean; headless smoke 0 JS errors incl. the Add-model modal.** **rules-checker: FAIL on the first pass (3), then PASS after the fixes below.**
  - **DEVIATIONS recorded (spec-grounded — do NOT silently "fix"):** (1) **`total_params` is file-derived ONLY for DENSE** models (from `general.size_label`); a MoE `size_label` is an expert-config label ("128x9.4B") the GGUF spec does not decompose into total/active params, so for MoE `total_params`/`active_params` stay **editable curation** (the file's raw label is shown read-only as "Size (file)"). (2) **`min_vram_mb`/`min_ram_mb` stay curation hints**, NOT file-derived: a correct MoE minimum needs the CPU-expert-offload model (out of Phase-2 scope); `/inspect` shows the precise `estimate_vram_mb` value (fed the REAL header inputs + real size) as info + soft-prefills Min VRAM when empty, but does not overwrite the curated offload-aware minimum. `coarse_fit` (`api.py:41`) reads the stored `total_params` — now file-derived for dense. (3) **Samplers persist at DOWNLOAD** (`set_derived` in `identify_fn`), not on the Add PUT: the Add form PREVIEWS them from `/inspect` (read-only) and they're captured from the local file at download — timed for their Phase-5 Lab consumer.
- **Phase 3 — MTP: detect + default-OFF + measurable (fixes MoE+MTP):** ✅ **DONE (2026-07-03).** MTP is now opt-in + measurable, NEVER auto-enabled. `switch_resolve.py` `resolve_model_switches`: the auto-`mtp` layer is DROPPED — resolution is `base → type(moe|dense) → per-hardware` only. This kills the old `mtp != "moe"` skip (which wrongly disabled MTP for MoE) AND stops dense+MTP models auto-drafting: `spec_type` stays its `knob_catalog` default (`none`) unless the user opts in per-Task in the Lab or per-machine via a `hardware_switch`. `seed.py`: the orphaned `mtp` switch-preset (`applies_to="mtp"`, `spec_type=draft-mtp`+`spec_n_max=3`) is REMOVED — those values live ONCE in the `knob_catalog` (`spec_type` enum + `spec_n_max`, both already seeded), so enabling MTP reads the defaults, no hardcoded duplicate. `switch_resolve.py` / `install.py` `switches_fn` / `db.py` `SwitchPreset` docstrings updated (no more base→type→mtp→hardware / `applies_to="mtp"` claims). **Surfaced as measurable:** `model_catalog_api.py` `ResolvedSwitchesResponse` gains `mtpCapable` (from the model's `mtp` catalog flag); the shared `switchResolve.js` exposes `resolveModelSwitches(id) → {switches, mtpCapable}` (one call, one source; `fetchResolvedSwitches` kept as the rows-only shim); `ConfigColumn.vue` (Lab) + `LuModelCatalog.vue` (Tune & measure) show a "this model supports MTP — set Speculative decode to 'MTP draft' and measure; gains are machine-dependent" hint for MTP-capable models, pointing at the existing `spec_type` Plane-1 knob (which already rides `engine_presets` — no new storage/dispatch). **Verified: runner 222 pytest + ruff green (`test_switch_resolve` rewritten — no-auto-mtp + a MoE CAN opt into draft-mtp via a `hardware_switch`; `test_switch_presets` "mtp" assertion updated); JW build:vite clean; headless smoke 0 JS errors AFTER a drop+reseed (the stale pre-Phase-2 dev DB lacked `trained_ctx` → exactly the documented no-migration reseed, confirmed not a code bug). rules-checker: FAILed the first pass on T5 (the de-mtp comment cleanup was incomplete) → fixed via a strict-diff over EVERY `mtp` reference (~11 stale preset-layer comments/docstrings across `switch_presets_api` / `seed` / `process` / `config` / `schema` / `lifecycle` / `db` / `stores` / `test_runner` / `model_catalog_api`) → re-run PASS.**
  - **`spec_type` now has ONE source (the rules-checker's T3 note, applied):** the `moe` preset's `spec_type=none` was DROPPED — it duplicated the `knob_catalog` `spec_type` default (`none`) + risked drift; the `dense` preset had no such row, so it was a pre-Phase-3 vestige, not a uniform "explicit off" policy. The `moe` preset keeps ONLY `no_mmap=true` (genuinely MoE-specific — no knob default). So NO model carries `spec_type` in its resolved baseline; the knob default (`none`) applies uniformly, and every MTP-capable model surfaces the opt-in via the `mtpCapable` UI hint. (Tests updated: `test_switch_presets` moe = `{no_mmap}`; `test_switch_resolve` MoE has no `spec_type` unless a `hardware_switch` sets it.)
- **Phase 4 — The unified "Models" surface (grid + folded-in catalog):** ✅ **DONE (2026-07-03).** Placement = a top-level **"Models" tab** (user pick). Body reconciled to unify-now + a 3-checker panel folded (2 blocking chat-default fixes + watch-items) BEFORE code. **Built:** **4a** (runner backend) — `seed.py` gains `DEFAULT_HARDWARE_TIERS` (9 tiers reproducing the research matrix), `TASKKIND_FUNCTIONS` + `function_of()` (chat/prose/extract/analysis + `other` + `embed`), an `embed` rec row (`nomic-embed-text`), the chat re-rank (27B r10 ceiling · **NEW** 35B-A3B r12 floor-quality · gemma r15 · 9B r30 faster); a pure `build_recommendation_grid()` (new `recommendation_grid.py`, `coarse_fit` per tier×function, quality/faster, columns from the DB task catalog) + `make_recommendation_grid_router()` (new `recommendation_grid_api.py`, `GET /v1/ai/recommendation-grid`, read-time VIEW, NO schema change) wired in `install.py` (lazy `_grid_margin` from the runner service). **4b** (reuse) — `useRunnerModels.js` (module-singleton: models + refresh + self-managing poll + load/unload + progress + engine-needed; NOT usePoll, which can't bind onUnmounted at module scope) + `TuneMeasureModal.vue` extracted OUT of `LuModelCatalog`; the flat catalog refactored to CONSUME both (the `busy` split kept `del:`/reset local, load/unload → the shared `loadingId`); `.lu-fit*` MOVED to shared `common/styles.css`. **4c** (fold-in) — new `RecommendationGrid.vue` (rows = tiers, detected box highlighted via `/hardware`; cols = functions; each cell = quality + faster `GridPick` with the Fit badge + why + Download/Load + Tune, live status from the shared singleton); `AiModelsArea` renames the "Recommendations" tab → **"Models"** hosting `<RecommendationGrid>` + the relocated `<LuModelCatalog>` ("Manage all models") + `<RecommendationsEditor>` under an "Advanced" `<details>`; `ProviderForm` drops the catalog, keeps `<LuRunnerEngine>` + a Models-tab pointer. **4d** (JW) — `seed_presets.py` `p_chat.model` → `qwen3.6-35b-a3b-mtp` (best-quality-at-floor; flagged for review). **4e** — quality/faster = display only. **Help:** new `justwrite-app/docs/models.md` + `toc.json` entry. **VERIFIED (JW-only):** runner **ruff + 231 pytest** (8 new grid tests incl. the floor-chat-convergence + RAM-gate-unlock + `other`-bucket + no-fabrication; `test_recommendations_catalog` chat-order updated); JW `build:vite` clean; **live curl** of `/v1/ai/recommendation-grid` on a reseeded DB confirmed functions=`[chat,prose,extract,analysis,embed]`, **vram8/chat quality=35B-A3B == the reseeded `p_chat`**, vram24/chat=27B, ram96/prose=235B, ram64/extract=GLM-Air, embed=nomic, AND `p_chat`=35B-A3B; **headless smoke 0 JS errors** incl. the Models tab + two new probes (`models-tab grid/catalog/embed-col/35b-in-grid/add-modal` ✓ · `provider-form engine/pointer/no-catalog` ✓); JW server ruff + **77 pytest**. **Reseed REQUIRED** (the chat re-rank + embed/35B rows + `p_chat` are merge-by-id — verified by dropping the dev DB + rebooting). JV inherits (additive-only; full `llm_runner` import clean; JV mounts the runner router directly, never `install_llm`, so it doesn't mount the grid router). **📌 p_chat=35B-A3B is a default-behaviour change — flagged for the user's review (one-line revert to `qwen3.5-9b-q4_k_m` if they prefer the fast 9B for chat latency; the 9B stays the grid's chat "faster" pick either way). → ✅ USER CONFIRMED 2026-07-03 (Phase 6): KEPT `qwen3.6-35b-a3b-mtp` — not reverted; decision closed.** **Post-task rules-checker: COMMIT-READY** (no blocking failures; all T1–T12 PASS/NA — reuse crux sound, chat convergence unit-asserted at the production margin). Folded its one nit: **`.lu-pill*` ALSO moved to shared `common/styles.css`** (it had drifted `2px 8px` vs `2px 9px` across the grid + catalog — same one-badge convergence as `.lu-fit`) + the "meaningfully lighter"→strict-`<` docstring. **Deferred non-blocking:** the pre-existing `QuickSetup.vue` `.lu-fit*` fork (different metrics; out of Phase-4 scope — #25/convergence follow-up); the smoke soft-logs `35b-in-grid`/`you-row` (pytest + the live curl are the hard convergence gate). **✅ COMMITTED + PUSHED: runner `7a0fa83`, JW `2e2cdb2`; both clean + synced.**
- **Phase 5 — Tune & measure → Tasks Lab handoff + samplers seed-and-show:** ✅ **SHIPPED (2026-07-03) — runner `50b7a04`, JW `ee92054`; both clean + synced.** Both parts built per the RECONCILED + PANEL-FOLDED design above (all 6 panel findings folded; a post-task rules-checker PASSED every rule, its one flag — this LIVE STATUS being stale — fixed + re-checked PASS before commit). **Part A (handoff):** new `ui/src/common/services/labHandoff.js` singleton + `sendToTasksLab`/`takeLabHandoff`; `AiModelsArea` tab → shared `activeAiTab` (computed get/set); `TuneMeasureModal` "Send to Tasks Lab" button; `CompareStrip` `defineExpose({addColumn})`; `FeatureLab` seeds the tuned column (idempotent, survives the `:key=testAgainst` remount; pins the bundled runner by `providerType==='local-llamacpp'`); `TaskKinds` consumes the handoff, lands on tasks[0], mounts the Lab even member-less (restructured guard + hint). **Part B (samplers seed-and-show + the ship-stopper fix):** `identity.py` `canonicalize_sampler_names` (`temp→temperature`, `penalty_repeat→repeat_penalty`, `penalty_last_n→repeat_last_n`) at all 3 producer sites → stored + inspect + resolver speak catalog names (fixes the silent-no-op the run path would have produced); plane-neutral rename `/model-catalog/switches`→`/resolved-defaults`, `ResolvedSwitchesResponse`→`ResolvedModelDefaultsResponse` (+`samplers`), `ResolvedSwitch`→`ResolvedFlag`, `switchResolve.js`→`modelDefaults.js` (`resolveModelDefaults` returns `{switches,samplers,mtpCapable}`; dead `fetchResolvedSwitches` dropped); `ConfigColumn` ONE `seedFields(gridKey,srcKey,rows)` guard for both grids, samplers minus temperature+top_p, `top_p`→params-row `topP` when blank, `onEditSamplers`/`writeOrder`/`writeStop` tag `samplersSource:'user'`, `patchPin` both sources 'model'; `CompareStrip.presetToConfig` tags `samplersSource:'preset'`. **VERIFIED (JW-only):** runner ruff + **233 pytest** (3 new — rename endpoint, samplers passthrough, canonicalize map; 5 identity tests updated to canonical names, one now exercises `penalty_repeat→repeat_penalty` end-to-end); JW `build:vite` clean; **live curl** — `GET /v1/ai/model-catalog/resolved-defaults` = 200 with `switches`+`samplers`+`mtpCapable`, old `/switches` = 404; **headless smoke 0 JS errors** over every route + all 6 AI sub-tabs + the Phase-4 + sampler-order probes; a **new Playwright probe** (`justwrite-app/scripts/phase5-handoff-probe.mjs`) drives the shared singleton → asserts the Tasks Lab seeds a NEW column (1→2, compare-not-clobber) carrying the tuned `ctx_len=4096`, **PASS, 0 non-benign errors**. Backend import + canonicalization confirmed live. **Deferred-verify (honest):** the full UI Tune-button→Send leg needs a downloaded model (not in dev) — the consumer half is probe-verified, the button leg is smoke-verified (0 errors) + on-device; the sampler grid VISUAL seed needs a model with `model_samplers` rows (download-time) — its data path is pytest-verified, its seed logic mirrors the shipped switch seed.
- **Phase 6 — Docs + final verify:** ✅ **DONE (2026-07-03).** Doc-only (no code). **Research-doc reconciliation** — `2026-06-27-model-catalog-research-and-recommendations.md`: the "4 jobs" (`:66` embeddings note) vs the 5-row routing matrix (`:71`) is reconciled — the 5 rows = the **4 JW functions** (chat/prose/extract/analysis) **+ Attribution, a JustVoice task** (`speaker_attribution`; JW's `CLAUDE.md` bans speaker analysis — the feature is JV §G / Part 3.3). Current JW routes **4 functions + `embed`** (the Phase-4 Models-grid columns, `seed.py` `function_of()` with an `other` bucket); the Attribution row informs **JV** only. Added the **honesty note** that the per-tier picks are a 3-reviewer panel + user reasoning, **NOT measured benchmarks (#28 open)** — and that **Tune & measure → Tasks Lab (Phase 5)** is now the path to ground them per-machine (tune switches → measure real tok/s → Send to Tasks Lab → save as the task's preset). **Cross-checked** the matrix against its folded copy in `2026-06-28-MASTER-PLAN.md` (the "Per-job × per-tier routing matrix" section, ~`:1460`) — **byte-identical** (grep-verified: research `:80-84` == master `:1467-1471`, Attribution row incl.; the separately-numbered abbreviated `§3.1` at ~`:5609` is a DIFFERENT condensed copy, not this one); added the same reconciliation note there (the master stays authoritative for the model research → kept in sync). **📌 p_chat DECISION CLOSED (user, 2026-07-03): keep `qwen3.6-35b-a3b-mtp`** (the smarter-chat default) — **NOT reverted** to the fast 9B; the Phase-4 flag is resolved. **Final verification sweep (after the container-restore recovery — the container came back on an old snapshot; both clones fast-forwarded to origin, no work lost):** runner ruff + **233 pytest**; JW `build:vite` clean; **headless smoke 0 JS errors** (every route + all 6 AI sub-tabs + the `models-tab`/`provider-form`/`sampler-order` probes); the **Phase-5 handoff probe PASS** (seeds the tuned column with `ctx_len=4096`, 0 non-benign errors); live curl of `/v1/ai/model-catalog/resolved-defaults` on a fresh reseed. **ALL PHASES 1–6 GREEN. ✅ COMMITTED + PUSHED: runner `67d7a61`, JW `843064f`; both clean + synced with origin.**
- **✅ Phase 1 SCOPE EXPANDED (2026-07-03) — model-recommended sampler capture folded in; ALL sampler decisions SETTLED:** Option B · Option 2 (read from file) · task-wins-per-knob (temperature task-owned) · **seed-and-show** (model values seed the visible Lab grids like switches — seen=run) · **OPEN-A → unify now** (grid = single model surface) · **OPEN-B → (a)** (temperature per-task, Option 2 fills the rest). Full detail in "## Phase 1 EXPANSION" below. **READY TO BUILD.**
- **📌 STANDING RULE (user, 2026-07-03):** update docs (this plan's LIVE STATUS + full-detail phase notes + `justwrite-app/MORNING_RECAP.md`) IN FULL DETAIL after EVERY phase, so a compaction/restart never loses progress.

---

## Phase 1 EXPANSION — read the model's recommended sampler settings from the file (added 2026-07-03; sampler decisions SETTLED 2026-07-03)

**Why this is part of Phase 1.** Phase 1 stalled on the *data* question — *which* GGUF facts we read and *why*. A model's recommended sampler settings are one of those facts. The user confirmed (2026-07-03, "B") that reading them is part of Phase 1's "read model facts from the file" mandate, not a separate feature. This section records the sampler decisions settled this session and the ones still open, so nothing is lost across a restart.

**Goal (user's words):** the best sampler settings for the model *and* the task. Wrong settings degrade output (too hot → rambling/hallucination; too cold → repetition); each model's maker publishes a recommended baseline.

**Verified in code this session:**
- Our per-task/per-feature config sets **temperature only** (+ `think` + `json_mode`) — `justwrite-app/server/justwrite_server/seed_feature_prompts.py`: `temperature` per feature (extraction 0.15, prose 0.7, …), **zero** non-temperature sampler keys (grep count = 0). `top_k`/`min_p`/`top_p`/penalties sit at ONE generic default for every model (`llm_runner/llm/seed.py:266-310`; engine presets seed empty).
- So today: per-**task** sampling ✅ (temperature); per-**model** sampling ❌ (the gap).
- `general.sampling.*` = 12 keys, verified literally in llama.cpp `gguf-py/gguf/constants.py` (temp/top_k/top_p/min_p/penalty_repeat/penalty_last_n/xtc_*/mirostat*/sequence); PR #17120 populates them from `generation_config.json` at GGUF-conversion time.
- `generation_config.json` lives in the model's ORIGINAL repo (live-probed: Qwen original → 200 with temp 0.7/top_p 0.8/top_k 20; GGUF quant repos → 404; Llama/Gemma → 401 gated; Mistral → present but no sampler keys). No single source covers every model.
- Catalog = 11 UNMEASURED example models (`seed.py:101-140`; "MEASURED tok/s still pending #28" `:164`); recommendations linked by `model_id` STRING, no FK (`db.py:156`); Phase 4 grid = a read-time VIEW joining recs × live fit (no merge/replacement).

**DECIDED this session (user-confirmed 2026-07-03) — do NOT re-litigate:**
1. **Option B** — sampler capture is part of Phase 1.
2. **Option 2 — auto-read the model's recommended sampler settings FROM THE FILE**, not hand-curated per model. Option 3 (hand-curate per built-in model) REJECTED: (a) the catalog is an unmeasured example set — curation would be throwaway; (b) hand-curating model facts is the exact anti-pattern this plan kills (root cause: "model facts are hand-typed, not read from the file"). Reading samplers from the file = the same principle as ctx/MTP/type.
3. **Base = generic defaults + per-task temperatures stay the backbone**; model-recommended values are ADDITIVE, never a replacement.
4. **Task wins per-knob** — the task owns temperature (and any knob it sets); the model fills the SECONDARY knobs the task leaves blank (`top_k`/`min_p`/`top_p`/penalties). Temperature stays task-driven (the model does NOT move temperature — the simpler branch, chosen this session).
5. **Sources + precedence:** `general.sampling.*` from the GGUF header when present → else fetch `generation_config.json` from the ORIGINAL repo → else generic default. **No `huggingface_hub` dependency** — reuse the runner's own `models.py` HF fetcher (a one-file GET).
6. **Keep `tokenizer.chat_template`** (+ `chat_template.{name}` / `chat_templates`) — reason to document: so the catalog can display/verify which chat format a model uses.
7. **Save-grid rule:** read every useful GGUF field except the token/tokenizer blobs (the big arrays).
8. **Model-card "best used for" tags → Phase 4** (recommendation grid), not the sampler work.

**Storage + display — SEED-AND-SHOW (corrected 2026-07-03; answers "new task preset or overwrite the existing one?"):**
- The model's recommended samplers are a per-MODEL **fact**, read from the file, stored on the model (`model_catalog` / a small side table), read-only "auto-detected from the file" — like `type`/`mtp`/`trained_ctx` in Phase 2.
- They are made **VISIBLE, not hidden**: when a model is selected in the Lab, they **SEED the sampler grid** (labeled "from the model") — exactly as the model's switch baseline already seeds the switch grid via the shipped model→switch-connect feature (`ui/src/switchResolve.js`, `ConfigColumn.vue` `switchesSource`, commit `2b0543f`). **What you see in the grid is what runs.**
- A preset bundles **model + switches + samplers** (`EnginePreset.model` `db.py:359` + `engine_preset_switches` + `engine_preset_samplers`; `presets_api.py:4`). Changing the model re-seeds both grids. **Update** overwrites the task's current preset in place; **Save as** makes a new named preset (then **Use in production** selects it, Plan A) — either way the preset captures exactly the shown values, and dispatch reads the preset (`_plane2_extra`). **Seen = saved = run.**
- **Correction of the earlier draft:** a prior version of this section said the model values were "layered read-only UNDER the task preset at dispatch, never shown / no preset written." That was WRONG — invisible runtime substitution is the exact confusion we are avoiding. The model values are SHOWN (seeded into the visible grid) and captured into the preset, matching how switches already work. The final dispatch mechanic mirrors the switch resolver so the two planes stay consistent.

**RESOLVED decisions (user-confirmed 2026-07-03) — formerly open:**
- **[OPEN-A → RESOLVED: UNIFY NOW]** The per-hardware recommendation grid becomes the **single model surface**: rows = hardware tiers, columns = functions (chat/prose/extract/analysis/embed), each cell = the model(s) that fit + a **Download** button + the cited "why"; the flat catalog list **folds into it**; once installed, a model exposes its load/tune controls there; manual **"Add your own GGUF"** stays for models outside our list. **This SUPERSEDES the plan's earlier "additive; defer the flat-list merge" (Phase 4 §Decision 5 / the "deferred follow-up" note).** Phase 4's body is rewritten to the unified surface when Phase 4 is built (per the "update docs after every phase" rule). Verified basis: catalog + recommendations are two tables joined by `model_id` string, no FK (`db.py:156`); the grid is already a read-time VIEW over them + live fit — folding the flat list in is a UI unification, not a data-model change.
- **[OPEN-B → RESOLVED: (a)]** **Temperature stays the ONLY per-task sampler**; Option 2 fills the secondary knobs (`top_k`/`min_p`/`top_p`/penalties) from the model file. Rationale (verified reasoning): there is **no good per-*task* secondary-knob data to hand-type** — community lore is unmeasured/inconsistent; model files are per-*model* (that IS Option 2, not per-task); our own measurement is #28 (not done). At low temp (extraction/structured) the secondary knobs are near-inert; for prose the best value is model-dependent, so Option 2 (the model's own file value) beats a hand-typed guess. Hand-typing per-task secondary values = more unmeasured hand-typed data = the anti-pattern this plan kills. Real per-task tuning, if ever wanted, goes through the **Tune & measure** harness (Phase 5) — measured, not guessed. *(Aside, NOT per-task: a sensible GLOBAL `min_p ≈ 0.05` default may be worth a look separately — Option 2 overrides it with the model's own when present.)*

---

## Phase 1 — Read GGUF metadata from the link, pre-download (foundation)

> **✅ KEY NAMES VERIFIED (2026-07-03) against 2 real headers** (range-read, `scratchpad/verify_gguf_keys.py`, our own parser — no assumed keys): **Qwen3.6-27B** (arch `qwen35`, dense+MTP) → `qwen35.nextn_predict_layers=1`, `qwen35.context_length=262144`, `general.file_type=15`, **`general.sampling.{temp=1.0, top_k=20, top_p=0.95}` PRESENT**, `general.base_model.0.repo_url`→`Qwen/Qwen3.6-27B`, `tokenizer.chat_template`; NO `expert_count` (dense). **GLM-4.5-Air** (arch `glm4moe`, MoE+MTP) → `glm4moe.expert_count=128`, `glm4moe.expert_used_count=8`, `glm4moe.nextn_predict_layers=1`, `glm4moe.context_length=131072`, **`general.sampling.*` EMPTY** (→ generation_config.json fallback). **Confirms:** MTP = `<arch>.nextn_predict_layers`; MoE = `<arch>.expert_count`/`.expert_used_count`; `general.sampling.*` is real but patchy (Qwen yes / GLM no) → the header→generation_config→generic precedence holds; the arch prefix is dynamic (read from `general.architecture`).

**Why:** everything downstream needs the file's facts *before* a multi-GB download.
**Grounding (corrected 【panel T2】):** two sources, different coverage —
- **HF API** `GET https://huggingface.co/api/models/{repo}` → `gguf` block gives `context_length`, `architecture`, **`total` = real file size** (+ file listing). Live-verified this session on Qwen3.6-27B-MTP / DeepSeek-V3 / GLM-4.5-Air — it does **NOT** expose `nextn_predict_layers`/`expert_count`.
- **Range-read of the GGUF binary header** (HTTP `Range` on the first ~1–2 MB of the `.gguf`) → the FULL per-arch KV incl. `nextn_predict_layers` (MTP), `expert_count` (MoE), `expert_used_count`, `block_count`, heads, `context_length`. This is the **primary** source for `mtp`/`type`/experts.

- **Extend the reader we already have** — `llm_runner/runner/gguf.py`. `read_gguf_metadata` already loads the whole KV header into `kv` (`:96-98`) but `GgufMeta` (`:41-47`) surfaces 6 fields. Add + populate in the `return` (`:106-113`): `context_length`, `nextn_predict_layers` (0 default → MTP signal), `expert_used_count`, `file_type`. **Verify each key name against a real MTP GGUF header before wiring** (the identity.py lesson — do not assume a key).
- **New remote fetch** — a small `gguf_remote.py`: `fetch_gguf_meta(hf_repo, quant) -> (GgufMeta, size_bytes)`. HF API for `context_length`/`architecture`/`total`; **range-read the header bytes into `io.BytesIO` and parse with the EXISTING `read_gguf_metadata`/`_read_value`** (one parser, local + remote). 【panel T4】 Options-considered: `huggingface_hub`/`gguf` PyPI can parse remote GGUF, but reusing our own `_read_value` adds **no new dependency** and keeps one parser — chosen for that reason.
- **Feed `fit.py` with real numbers (keep it — #29)** 【panel B-minor】: `coarse_fit` (`fit.py:75`) is params/quant-based → feed it the **real params** from the header; the **real file `total` size** (HF API) feeds the precise VRAM path (`weights_mb`/estimate), replacing the hand-typed `min_vram`/`min_ram` guess. No new fit math.

**Files:** `llm_runner/runner/gguf.py`, new `llm_runner/runner/gguf_remote.py`, touch-point into `fit.py` (inputs only).

## Phase 2 — Auto-derive catalog fields; the file is the source of truth
**Why:** kill the hand-typed drift. Two-phase, mirroring how `type` already works: **seed = pre-download guess; the header overwrites at add + download.**

> **✅ BUILT 2026-07-03 — full built detail + the 3 spec-grounded DEVIATIONS are in the LIVE STATUS "Phase 2 … DONE" line above.** Key correction folded from the POST-build rules-checker (which FAILed the first pass): `general.size_label` IS a real GGUF key (verified vs docs/gguf.md + 2 real headers) — dense param count ("27B") is file-derived into `total_params`; MoE ("128x9.4B") is an expert-label that does not decompose (params stay curation). The touch-list below is amended to include the per-model **`model_samplers`** storage (omitted in the first draft — the "store the sampler fact in Phase 2" decision from the EXPANSION + task #105).

> **Phase-1 carry-forward — LANDED 2026-07-03 (was non-blocking):**
> 1. **`fit.py` feed MUST land here.** The `fit.py` "inputs-only" touch-point (deferred from Phase 1 — no consumer yet, would be dead code) lands with the new `/inspect` endpoint: `fetch_gguf_meta` → feed `coarse_fit` the header's real params + the real `total` size (replacing the hand-typed `min_vram`/`min_ram`), and feed `estimate_vram_mb`/`max_gpu_layers` (`fit.py:144-166`) the real `size_mb`/`n_layers`/`n_kv_heads`/`embedding_dim`/`ctx_size`. If it does not land here, the plan's Phase-1 "fit.py (inputs only)" requirement silently evaporates.
> 2. **`total_weight_bytes` excludes the mmproj sidecar** (`gguf_remote.fetch_gguf_meta` passes `mmproj=None`) — when `total` feeds fit/UI here, decide whether to add the sidecar size for multimodal MoE GGUFs.
> 3. **Cheap test hardening** for the `gguf_remote` branches the Phase-1 suite monkeypatched over: (a) truncated-first-read → the 4× retry succeeds; (b) a fake response returning > n bytes → the read stops at n (the CDN-ignores-Range guard); plus `gguf.py` bool-exclusion filter + nested-array skip-branch coverage.
> 4. **Shard pick** uses `"00001" in path` — correct for the llama.cpp `-00001-of-000N` convention + single-file repos; harden to `-00001-of-` if a non-padded split ever appears.
>
> **STATUS 2026-07-03 (post-build rules-checker):** #1 LANDED with a spec-grounded deviation — `estimate_vram_mb` IS fed the real header inputs + real size in `/inspect`; `coarse_fit` now reads the file-derived `total_params` (dense, from `general.size_label`); `min_vram`/`min_ram` stay curation because a correct MoE minimum needs the offload model (out of scope) — recorded, not silently dropped. #2 mmproj: `total` is weights-only; a multimodal-MoE sidecar add stays open (no multimodal model in the catalog yet). #3 DONE — 4 edge tests added (truncation-retry, CDN-range-stop, bool-exclusion, nested-array skip). #4 unchanged (`"00001" in path` adequate; hardening deferred). First-pass FAIL cause: I asserted "no GGUF param field" from our OWN partial parser instead of the spec — corrected by verifying `general.size_label` at docs/gguf.md + 2 real headers.

- **`db.py`** (`:69-101`): add `trained_ctx` (Integer, nullable = header `context_length`) on `ModelCatalog` **+ a new `model_samplers` child table** (model_id, param_name, value; no FK, like `ModelRecommendation`) for the per-model recommended-sampler FACT (the EXPANSION + task #105 "store the sampler fact in Phase 2" decision). Keep `mtp`/`type` (already columns). Drop+reseed (no migration — see the top-banner schema note).
- **`identity.py`** — a new `derived_fields_from_meta(meta)` maps ONE header read → `{type, mtp, trained_ctx, total_params (dense-only via `general.size_label`), size_label, samplers}`; `detect_and_store_model_type` (post-download) writes them via a new `store.set_derived(...)` (+ an optional `samplers_fallback`); a new `inspect_model_from_link(repo,quant)` does the PRE-download read (range-read header → same facts + real size + `estimate_vram_mb`). **Docstring fixed 【panel T11】** ("mtp NOT inferred / no signal" → now inferred from `nextn_predict_layers`).
- **Hydrate on ADD (pre-download)** — the add-a-model flow (`LuModelCatalog.vue` `startAdd`/`saveModel`): a new `POST /v1/ai/model-catalog/inspect?repo=&quant=` calls Phase-1 `fetch_gguf_meta` → fills `type`/`mtp`/`trainedCtx`/params/real-size **before** download. `identify_fn` (wired `install.py:151-157`, called `lifecycle.py:438`) re-confirms from the local file at download.
- **Wire through** — `model_catalog_api.py` `CatalogRow` (`:34-49`): add `trainedCtx`. `stores.py` both directions (`_catalog_to_wire` `:342`, upsert `:372`).
- **`LuModelCatalog.vue` Edit form** (`:421-448`): file-derived fields (`type`, `mtp`, `trainedCtx`, params, quant) → **read-only "auto-detected from the model" + "revert to file"**; hand-editable = curation/policy only (name, repo/quant, `useLimited`, license). **In THIS phase 【panel T5】: fix the stale copy `:447`** ("Speculative decode (MTP) — not GGUF-detectable" → detected from the header). Reuse kit `UiCheckbox`/`UiInput`/`UiField` + the `.lu-mm-adv` disclosure.

**Files:** `db.py`, `identity.py`, `stores.py`, `model_catalog_api.py`, `LuModelCatalog.vue`; tests `tests/test_identity*.py`.

## Phase 3 — MTP: detect + default-OFF + measurable (fixes MoE+MTP)
**Why:** MTP is machine-dependent — offload +16% / full-GPU RTX 3090 *slower* (research `:89`); Metal net-loss at every config (web #23752/#23184, not in the doc cite). So detect the capability from the file (Phase 2), **never auto-enable**, surface it as a measurable switch. Removing the auto-apply also cleanly deletes the (intentional-but-superseded) MoE-skip.

> **✅ BUILT 2026-07-03 — full built detail is in the LIVE STATUS "Phase 3 … DONE" line above.** Built as specified; the `spec_type`/`spec_n_max` knobs already existed in `knob_catalog` (no knob added). The `moe` preset's `spec_type=none` was DROPPED (rules-checker T3 note — it duplicated the knob default + risked drift; the moe preset keeps only `no_mmap`), so `spec_type` has ONE source. MTP surfaced via a new `ResolvedSwitchesResponse.mtpCapable` + a Lab/Tune hint on the existing `spec_type` knob.

- **`switch_resolve.py` `resolve_model_switches`** (`:47-54`): **drop the auto-`mtp` layer** — apply `base → type(moe|dense) → per-hardware` only. Kills the `mtp != "moe"` skip (`:53`) + honors default-OFF. **In THIS phase 【panel T11】: update the `switch_resolve.py:8-14` module docstring + the stale `install.py:143` `switches_fn` comment** (both still describe base→type→mtp→hardware).
- **Remove the now-orphaned `mtp` switch-preset** 【panel T3】: `seed.py:157-158` was the SOLE consumer of `applies_to="mtp"`; once resolve drops it, it's dead. Delete it. The **MTP profile values live once in `knob_catalog`** — the `spec_type` enum (`seed.py:260-262`, `none|draft-mtp|ngram-mod`, default `none`) + a `spec_n_max` knob (default `3`, add if absent). The opt-in reads those defaults → **one source, no hardcoded duplicate**.
- **Surface the switch** — in `ConfigColumn.vue` (Lab) + the Tune grid (`LuModelCatalog.vue`), when the model is **MTP-capable** (`mtp` flag), show the `spec_type` switch (hint: "this model supports MTP — measure it") default `none`; enabling writes `draft-mtp`+`spec_n_max` into the preset (rides `engine_presets` — no new storage/dispatch).
- **Tests** — `tests/test_switch_resolve.py`: MTP no longer auto-applied; a MoE model can carry `spec_type=draft-mtp` when set.

**Files:** `switch_resolve.py`, `seed.py`, `install.py` (comment), `ConfigColumn.vue`, `LuModelCatalog.vue`, `tests/test_switch_resolve.py`.

## Phase 4 — The unified "Models" surface: a per-hardware recommendation grid + the folded-in catalog
**Why:** with live fit we can show, per hardware tier, which model serves each function — the per-job×per-tier matrix the research already built (`model-catalog-research-and-recommendations.md:71-89`, a research finding to seed the initial content, confirmed later by #28), now *live*. **OPEN-A resolved this to UNIFY NOW** (LIVE STATUS §"SCOPE EXPANDED" + the RESOLVED-decisions block): the grid becomes the SINGLE model surface — the two hand-curated places (the flat catalog under Providers → Built-in, and the Recommendations tab) **fold into ONE "Models" tab**. This SUPERSEDES the pre-reconciliation "additive; the catalog stays under Providers; flat-list removal deferred" body (Decision 5 struck below).

> **✅ RECONCILED to unify-now + placement DECIDED (2026-07-03).** Per the "rewrite Phase 4's body when Phase 4 is built" note (OPEN-A resolution). **Placement (user pick, 2026-07-03):** the unified surface is a **top-level "Models" tab** (the "Recommendations" tab renamed) — chosen over "keep it under Providers → Built-in". Providers → Built-in keeps ONLY the engine-install panel (`LuRunnerEngine`) and links to the Models tab; its flat model list moves into the Models tab. Grounded in a full read of the touched surfaces this session (`recommendations_api.py`, `runner/fit.py::coarse_fit`, `runner/api.py`, `seed.py`, `AiModelsArea.vue`, `RecommendationsEditor.vue`, `ProviderForm.vue`, `LuModelCatalog.vue`, `schema.py`, the research matrix). Sub-steps 4a–4e below.

### 4a — Backend: tiers + function map + embed recs + chat re-rank + the grid resolver (runner)
- **Hardware-tier bands — `DEFAULT_HARDWARE_TIERS` (runner `seed.py`) 【panel T1】:** the grid ROWS, seeded `[{key, label, vram_mb, ram_mb}]`, reproducing the research matrix columns (`model-catalog-research-and-recommendations.md:74`): CPU-only (0 / 32000) · 8 GB (8000 / 32000) · 12 GB (12000 / 32000) · 16 GB (16000 / 32000) · 24 GB (24000 / 32000) · 32 GB (32000 / 32000) · 64 GB-RAM (24000 / 64000) · 96 GB-RAM (24000 / 96000) · 128 GB workstation (24000 / 128000). The RAM pairing is what gates the MoEs via `coarse_fit`'s `min_ram_override`: the 35B-A3B (catalog `min_ram 32000`) fits from the 8 GB floor; GLM-4.5-Air (`64000`) unlocks at 64 GB-RAM; Qwen3-235B (`96000`) at 96 GB-RAM — matching matrix rows `:76-80` exactly. `coarse_fit(total_params, quant, vram_mb=tier.vram, ram_mb=tier.ram, margin_mb, min_vram_override, min_ram_override)` per cell (the pure fn `runner/fit.py:75`, **unchanged**).
- **Function map — `TASKKIND_FUNCTIONS` + `function_of()` (runner `seed.py`), ONE source 【panel T1】:** `{chat.grounded, chat.inVoice} → chat` · `{prose.generate, prose.edit, ideation} → prose` · `{extract.structured, summary.grounded, creative.structured} → extract` · `{judge.scored} → analysis` · `{embed} → embed`; ANY taskKind not in the map → the explicit **`other`** bucket (custom tasks / JV's future `attribution`) so nothing silently vanishes. The grid's COLUMNS = the functions actually present = `{ function_of(t.id) for t in task catalog } ∪ {embed}` (+ `other` only if a task maps there) — per-app, computed from the DB task catalog, no orphans. Map lives once in `seed.py`, not a component.
- **`embed` recommendation rows (runner `seed.py` `DEFAULT_RECOMMENDATIONS`) 【panel T5】:** add `{"model_id": "nomic-embed-text", "task_kind": "embed", "rank": 10, "why": "Local embedding model (~137M) — the RAG / semantic-search index; CPU-fine, Apache-2.0."}` — reuse the ONE `model_recommendations` table (no parallel source). `embed` is a pseudo-taskKind (NOT in `DEFAULT_TASK_KINDS`; it is the RAG role, mapped to the `embed` function). `nomic-embed-text` already exists in `DEFAULT_CATALOG` (`seed.py:137`).
- **Chat default → best-that-fits (runner `seed.py` `DEFAULT_RECOMMENDATIONS` chat rows):** re-rank chat.grounded so the best-QUALITY chat model that fits is the "quality" pick and the 9B is the lighter "faster" pick — from the current 9B=r10 / gemma-12b=r15 / 27B=r20 to **27B=r10 (the chat quality ceiling; wins where it fits, ≥16 GB) · `qwen3.6-35b-a3b-mtp`=r12 (a NEW chat.grounded row — the floor/12 GB quality pick, since 27B does NOT fit <16 GB but the A3B MoE does via `--n-cpu-moe`) · gemma-12b=r15 · 9B=r30 (fast/faster)**, and reword the 9B `why` to "fast, re-askable chat — the grid's 'faster' pick." **【panel-fix, checkers 2+3 T1】 The NEW 35B-A3B chat row is REQUIRED so the grid's FLOOR chat quality pick == the 4d `p_chat` default — else the shipped default is a model the grid never recommends for chat, breaking the "what you see in the grid is what runs" invariant (EXPANSION lines 59/61).** Verified against `coarse_fit` per tier: CPU/floor/12 GB → quality=35B-A3B(r12) + faster=9B; 16 GB+ → quality=27B(r10) + faster=9B. (The 2026-06-27 research defaulted chat to the fast 9B for latency; it EXPLICITLY sanctioned a user flip to a 35B-A3B "smarter chat" default — `model-catalog-research-and-recommendations.md:84` — which is the flip the plan takes, 9B one click away as "faster.") **【panel-fix, checker 3】 This RANK re-rank (rank changes on EXISTING rows) + the new embed/35B-A3B rows also need a reseed / `POST /v1/ai/recommendations/reset` to apply — the merge-by-id `seed_default_recommendations` skips existing `(model_id, task_kind)`; a stale-DB probe would silently pass on the OLD ranks — so 4e reseeds + asserts the NEW chat ordering.**
- **Grid resolver — pure + a router (runner) 【panel A-note: backend, NOT client】:**
  - a pure, unit-testable `build_recommendation_grid(catalog_rows, recommendations, tiers, function_of, margin_mb) -> GridResponse` in a NEW `llm_runner/llm/recommendation_grid.py` (NO I/O; the whole fit-truth is `coarse_fit`). For each tier × function: candidates = rec rows whose `function_of(task_kind) == function` AND whose catalog model `coarse_fit(tier) ∈ {ok, tight, cpu}`; sort by `rank` asc; **quality** = the top-ranked fitting model; **faster** = the lightest OTHER fitting candidate (by `fit.weights_mb`, meaningfully lighter) or none. **【panel-note, checker 1 T1】 `weights_mb` uses `total_params`, so for a low-active MoE it computes the FULL-param weight (may mis-order a low-active MoE vs a dense model) — a display-only ordering hint; comment it inline.**
  - `make_recommendation_grid_router(get_rec_store, get_catalog_store, *, get_margin_mb=None)` in a NEW `llm_runner/llm/recommendation_grid_api.py` (beside `recommendations_api.py`): `GET /v1/ai/recommendation-grid` → `build_recommendation_grid(...)`. `get_margin_mb` None → `DEFAULT_SAFETY_MARGIN_MB`; JW wires `lambda: get_service().config().safety_margin_mb`. **Recommendations keep `(modelId, taskKind, rank, why)` — NO schema change; the grid is a read-time VIEW.** The endpoint returns fit + why + quality/faster ONLY; the UI overlays live download/load status by `modelId` from the `/v1/llm-runner/models` it already holds (one status-truth, no duplication).
  - Wire the router in `install.py` (mount + pass the JW `get_recommendation_store`/`get_catalog_store` + the margin fn).
- **Wire shapes (`recommendation_grid_api.py`):** `GridPick {modelId, name, params, fit, rank, why}` · `GridCell {tier, function, quality: GridPick|None, faster: GridPick|None}` · `GridTier {key, label, vramMb, ramMb}` · `GridResponse {functions: list[str], functionLabels: dict[str,str], tiers: list[GridTier], cells: list[GridCell]}`.

### 4b — Reuse extraction so the grid + the flat catalog share ONE model-state (runner UI) 【RULE #3】
The grid needs live per-model status + Download/Load + Tune — all already in `LuModelCatalog.vue`. Extract them ONCE (matching the shipped `usePoll` / `switchResolve.js` / `dialog.js` shared-module precedent), so the grid and the flat list are ONE truth, not two pollers:
- **`ui/src/common/composables/useRunnerModels.js`** — the shared runner-models state as a module singleton: `models` + `refresh()` + the `usePoll` loop + `load(modelId)` / `unload()` + download-progress (`downloaded`/`total`/`detail`/`progressLabel`) + `loadErr`/`needsEngine`. `LuModelCatalog` refactors to CONSUME it; the grid consumes the SAME instance (no double-poll). **【panel-fix, checkers 1+2】 (a) `busy` split:** today `busy` is shared by `load`/`unload` (`LuModelCatalog.vue:155,166`) AND catalog `deleteModel` (`:366`, `del:`-namespaced). Move `load`/`unload`'s in-flight id into the composable (`loadingId`); KEEP `deleteModel`/`resetCatalog`'s `busy` local to `LuModelCatalog` — else the relocated row spinners desync. **(b) poll ownership:** `usePoll` registers `onUnmounted`, which won't bind at module scope — the composable exposes `start()`/`stop()` and the CONSUMING component owns start-on-load / stop-when-idle (the existing imperative `refresh()` start/stop, `LuModelCatalog.vue:136-143`, already gates this). **(c)** the extraction is a pure move (NO behavior change) — re-asserted by a dedicated Playwright probe in 4e (the smoke only proves zero JS errors, not behavioural equivalence).
- **`ui/src/components/TuneMeasureModal.vue`** — extract the Tune & measure modal out of `LuModelCatalog` (the `startTune`/`runMeasure`/`resetTuneSwitches` + `KnobGrid` + the MTP hint + the tok/s result) into a self-contained `<TuneMeasureModal :model @close>`. BOTH the grid and `LuModelCatalog` open it. (Keeps the Add/Edit + GGUF-inspect modal INSIDE `LuModelCatalog` — it is catalog-management, folded in with the flat list.)

### 4c — The unified "Models" tab (fold-in, Option 1) (runner UI)
- **New `ui/src/views/RecommendationGrid.vue`** — the grid: `GET /v1/ai/recommendation-grid` → rows = tiers (the DETECTED box highlighted via `/v1/llm-runner/hardware`), columns = `functions`, each cell = the quality + faster `GridPick`s with the fit badge, the cited `why`, and per-pick **Download/Load** (via `useRunnerModels.load`) + **Tune** (opens `<TuneMeasureModal>`) + live status (from `useRunnerModels.models` by `modelId`). Reuse kit (`UiButton`/`UiTag`/`UiProgress`) + `request()`. **【panel-fix, checkers 1+2 T3】 The fit badge `.lu-fit`/`.lu-fit--*` is `<style scoped>` in `LuModelCatalog.vue:576-583`, so `RecommendationGrid` CANNOT reach it — MUST **MOVE** it to shared CSS (`ui/src/common/styles.css`), not copy it into a second scoped block (the fork risk). Both surfaces then read the ONE badge.**
- **`ui/src/views/AiModelsArea.vue`** — rename the **"Recommendations"** subnav item + section to **"Models"** (`tab === 'models'`, subnav `:146`, section `:232-235`); the section hosts, in order: `<RecommendationGrid>` (primary) → `<LuModelCatalog>` (RELOCATED here as "Manage all models" — the full flat list + Add-your-own-GGUF + Edit/Delete/Reset, the literal "flat catalog folds in") → an **"Advanced: edit recommendations"** `<details>` disclosure wrapping `<RecommendationsEditor>` (demoted from its own tab). Both `<RecommendationGrid>` and `<LuModelCatalog>` share the `useRunnerModels` singleton (one status truth).
- **`ui/src/views/ProviderForm.vue`** — the built-in provider form drops `<LuModelCatalog>` (`:202`); KEEP `<LuRunnerEngine>` (`:201`, install the engine) + add a one-line "Manage models in the **Models** tab →" pointer. (Models now live in the Models tab; the engine you install stays on the provider.)
- **User-facing help for the new tab 【panel-note, checkers 1+2+3 T11】:** a NEW top-level "Models" tab lands its help in THIS phase (4c), not deferred to Phase 6 — add a JW `services/helpDocs.js` entry (+ `toc.json`) for the Models tab / the grid (what fit/quality/faster/Download mean). The endpoint stays self-documented in-code (Pydantic + FastAPI summaries).
- **Badge-consistency check 【panel-note, checker 1】:** the grid's fit comes from the catalog store (`CatalogRow.minVramMb/minRamMb`) and the relocated flat table's fit from `/v1/llm-runner/models` (`ModelEntry.recommendedFor.minVramMb` / `ModelEntry.minRamMb`). Both must trace to the SAME `model_catalog` columns via JW `stores.py`'s mappers — verify so the two badges on one tab never diverge.

### 4d — Chat preset off the 9B (JW)
- **`justwrite-app/server/justwrite_server/seed_presets.py` (`p_chat`, `:52-54`)** — flip `p_chat.model` off `qwen3.5-9b-q4_k_m` to **`qwen3.6-35b-a3b-mtp`** — the **highest-QUALITY** model that fits **at the floor** (~32B-class MoE via `--n-cpu-moe` at 8 GB VRAM + 32 GB RAM; catalog `min_vram 6000`/`min_ram 32000` → `coarse_fit`="ok"), so the chat DEFAULT is best-QUALITY-that-fits while STILL "runs for everyone" (the `seed_presets.py:34` floor invariant); the fast 9B stays one click away as the grid's chat "faster" pick. **【panel-fix, checkers 2+3 T2】 NOT "the only >9B model that runs at the floor" — that claim was FALSE: `gemma-4-12b` (`min_vram 7000`/`min_ram 13000`, `seed.py:105-107`) ALSO fits at the floor ("tight"). The true reason is QUALITY (35B-A3B ~32B-class-via-offload > gemma's 12B-dense; 27B, the chat ceiling, needs 16 GB+ so cannot be the floor default). The 2026-06-27 research explicitly sanctioned a user flip to a 35B-A3B "smarter chat" default (`model-catalog-research-and-recommendations.md:84`).** **DECISION flagged for review** (a one-line change if the user prefers a different chat default — e.g. keep the fast 9B for interactive latency, ~55 vs ~17-20 t/s). Paired with the NEW 4a 35B-A3B `chat.grounded` rec so the grid's floor chat quality pick == this default. Requires a dev reseed to take effect (seeders merge-by-id).

### 4e — "Quality vs faster" = display only
Two picks per cell; **NOT** a revived Fast/Balanced/Best dial (D3 stays dead). The picks are a display of the recommendation×fit view, not a new stored control.

**Files:** NEW `llm_runner/llm/recommendation_grid.py` (pure) + `llm_runner/llm/recommendation_grid_api.py` (router) + `DEFAULT_HARDWARE_TIERS` + `TASKKIND_FUNCTIONS`/`function_of` + embed row + chat re-rank incl. the NEW 35B-A3B `chat.grounded` row (runner `seed.py`) + `install.py` (wire) + tests (`tests/test_recommendation_grid.py`); NEW `ui/src/common/composables/useRunnerModels.js` + `ui/src/components/TuneMeasureModal.vue` (extract) + `ui/src/views/RecommendationGrid.vue` (new) + `ui/src/common/styles.css` (MOVE `.lu-fit*` here) + `ui/src/views/AiModelsArea.vue` (rename tab + fold-in) + `ui/src/components/LuModelCatalog.vue` (consume the composable + modal; relocated; `busy` split) + `ui/src/views/ProviderForm.vue` (drop catalog, keep engine + pointer) + `ui/src/views/RecommendationsEditor.vue` (demoted under the disclosure); JW `seed_presets.py` + `justwrite-app/src/renderer/src/services/helpDocs.js` (+ `toc.json`, Models-tab help).

**Phase-4 verify additions (fold into the Verification section):** (1) after the JW build:vite, **reseed** (drop the dev DB or `POST /v1/data/reset` — the chat re-rank + embed/35B-A3B rows are merge-by-id, they do NOT apply to existing rows) then headless smoke 0 JS errors over the renamed **Models** tab; (2) a Playwright probe asserting the **grid** renders per-tier with live fit + quality/faster + Download + the `embed` column, AND that the **floor chat quality pick == 35B-A3B** (the new ordering took, not stale ranks); (3) a Playwright probe re-asserting the **relocated `LuModelCatalog`** load/progress/unload/Tune still works after the `useRunnerModels`/`TuneMeasureModal` extraction (behavioural, beyond the JS-error smoke); (4) `ProviderForm` → Built-in shows the engine panel + the Models-tab pointer, NO catalog table.

> **✅ PANEL RECORD — Phase 4 (reconciled plan), 2026-07-03.** A 3-checker rules panel (architecture-fit · reuse/convergence · grounding) reviewed the unify-now reconciliation against the live code. **Architecture-fit: PASS.** **Reuse + Grounding: STOP-AND-FIX** on the SAME two items (unanimous), both in the chat-default sub-story — folded above: **(T2)** the "ONLY >9B at the floor" claim was FALSE (gemma-4-12b also fits) → reworded to best-QUALITY-that-fits; **(T1)** `p_chat`=35B-A3B had no `chat.grounded` rec → added the r12 row so the grid's floor chat quality == the shipped default (the "what you see runs" invariant). Non-blocking watch-items folded: reseed-applies-to-the-re-rank-too (+ a probe on the new ordering), the shared `busy` split, `.lu-fit*` MOVED to shared CSS, a post-extraction behavioural probe, the Models-tab help entry in-phase, the `weights_mb`-ignores-active_params display note, the badge-consistency check. **All three checkers verified the CRUX sound** — the pure resolver + injected router, the `coarse_fit` reuse (unchanged), the tier bands reproducing the research matrix, the function map (all 9 tasks + `other`/`embed`), the embed row, the reuse extraction (one poller, one status truth, one recs table), and every other file:line. **Post-fix: SOUND-TO-BUILD.**

## Phase 5 — Tune & measure → Tasks Lab handoff
**Why:** close the loop — tuned switches (incl. custom) shouldn't die in the measure modal. No active *feature* in the Tune context → hand off to a **task** (a task owns its preset — Plan A).

- **Shared handoff channel (kit)** — a module-level ref `labHandoff` (`ui/src/common/labHandoff.js`, matching the `dialog.js`/`toastBridge.js` singleton pattern): **`{ providerId, model, switches }`** — 【panel T1b】 includes `providerId` (the local llama.cpp provider) because a ConfigColumn pin is `{providerId, model}` (`ConfigColumn.vue:81-83`).
- **Tune modal (`TuneMeasureModal.vue` — extracted from `LuModelCatalog` in Phase 4b; the "Measuring only…" note is `LuModelCatalog.vue:524-527` PRE-extraction / in `TuneMeasureModal.vue` after)** — a **"Send to Tasks Lab"** action + make the "Measuring only…" note a **link** that (a) writes `{providerId, model, tuneRows}` (incl. custom rows) to `labHandoff`, (b) sets the AI tab to `tasks` (local ref `AiModelsArea.vue:33` — expose a shared `activeAiTab` setter). *(Line refs re-grounded 2026-07-03: the OLD `:459-499`/`:472-474` are the Add/Edit modal + the inspect button, NOT the tune modal — verify against `TuneMeasureModal.vue` when Phase 5 is built.)* Copy ladder: model-level (here) → **Tasks** → "for per-feature fine-tuning, use Routing by feature".
- **Landing = a new Compare column under the first task (user's call), made robust 【panel T1b】.** `TaskKinds.vue:103` already auto-selects `tasks[0]`; it **imports** `FeatureLab` at `:17` but **mounts it guarded** at `:296` (`v-if="testPrompt"`, `:289` needs a member feature). Since switch-tuning + Save-as-preset **don't need a test prompt**, the handoff must render/seed a Compare column even when `tasks[0]` has no members (relax the guard for the handoff column, or show a tuning-only column with an "assign a member to test the prompt" hint). On a pending `labHandoff`, `FeatureLab`/`CompareStrip.addColumn(seed)` (`:40-42`) adds a column seeded with `{providerId, model, switches}` tagged **`switchesSource:'user'`** (the guard from `ConfigColumn.vue:120-143`) — **alongside** the task's preset column (compare, not clobber) → **Save as the task's preset** (existing path, `FeatureLab.vue:81`).
- **Reuse:** `switchesSource` guard, `CompareStrip` column model, `FeatureLab` Save-as-preset, the tab ref. `labHandoff` is a new tiny singleton (no existing channel to reuse).

**Files:** new `ui/src/common/labHandoff.js`, `LuModelCatalog.vue`, `AiModelsArea.vue`, `TaskKinds.vue`, `FeatureLab.vue`, `CompareStrip.vue`.

> **✅ RECONCILED + 3-CHECKER PANEL FOLDED (2026-07-03) — pre-build; this supersedes the terse touch-list above.** Phase 5 is TWO decided pieces: **Part A — the Tune→Tasks handoff** (this §Phase 5) + **Part B — samplers seed-and-show** (the §"Phase 1 EXPANSION" SEED-AND-SHOW decision, whose Phase-2 storage explicitly deferred "their Phase-5 Lab consumer" — Phase-2 LIVE STATUS deviation #3). A 3-checker rules panel (architecture-fit · reuse/convergence · grounding) reviewed the execution plan BEFORE any code; **all three FAILed** and caught **two ship-stoppers**; all findings folded below. User confirmed (2026-07-03): build both A+B; run the panel first.
>
> **Part A — Tune & measure → Tasks Lab handoff (build detail):**
> - **NEW `ui/src/common/services/labHandoff.js`** (NOT `common/` — placed beside `dialog.js`/`toastBridge.js`, the singleton precedent the plan cites; doc path corrected per panel): module-level `labHandoff` ref (`{providerId, model, switches}` | null) + `activeAiTab` ref (default `"providers"`) + `sendToTasksLab({providerId,model,switches})` (sets both) + one-shot `takeLabHandoff()` (returns+clears).
> - **`AiModelsArea.vue:35`** `const tab = ref("providers")` → a `computed` get/set backed by the shared `activeAiTab` (the panel flagged that a raw imported-ref write `@click="tab='x'"` is ambiguous across the SFC-compiler binding boundary — `computed` is unambiguously a ref for read+write). Side effect (benign, documented): the tab now PERSISTS across `AiModelsArea` remounts.
> - **`TuneMeasureModal.vue`** (the tune modal, extracted from `LuModelCatalog` in Phase 4b — the plan's OLD `:459-499` refs are moot, re-grounded to `TuneMeasureModal.vue`): a "Send to Tasks Lab" action in the "Measuring only…" note (`:140-143`) → `sendToTasksLab({providerId:"", model:props.model.id, switches:tuneRows.value})` (`tuneRows` = `[{name,value}]`, incl. custom rows) + `emit("close")`. providerId is resolved on the CONSUME side (the modal has no providers list).
> - **`CompareStrip.vue`** — `defineExpose({ addColumn })` (`addColumn(seed)` at `:40` clones a seed into a new column; internal today).
> - **`FeatureLab.vue`** — new `handoff` prop + a `stripRef` on `<CompareStrip>`; seeds ONE column = `{...columnConfig.value, pin, switches:handoff.switches, switchesSource:'user'}` via `stripRef.addColumn(seed)` — ALONGSIDE the task's preset column (compare, not clobber). **providerId resolved to the bundled runner by `providerType==='local-llamacpp'` (id `local-llamacpp`), NOT `providers.find(p=>p.local)`** (panel G-T2b: TWO locals are seeded — `local-llamacpp` + `openai-compat-local`/Ollama, both `local:True` `seed.py:70-73` — first-local may grab Ollama, which does not own the runner-catalog model id → invalid pin). Seeds **idempotently on mount + on `handoff`-change** (survives the `:key="testAgainst"` remount).
> - **`TaskKinds.vue`** — import `labHandoff`/`takeLabHandoff`; after `load()` and via a watch, consume a pending handoff → `selectTask(tasks[0].id)` → set a `pendingHandoff` ref (passed to FeatureLab) held UNTIL the user manually picks a different task (NOT cleared on consume — panel A-T1: clearing on consume made the seeded column VANISH when the "assign a member" hint changed `testAgainst` and remounted FeatureLab). **Restructure the OUTER `v-if="selMembers.length"` gate (`:289`) + its `v-else` (`:302`)** to admit a member-less handoff (panel A-T1: relaxing only the inner `v-if="testPrompt"` still leaves the whole 289–301 block hidden), + a "assign a member to also test the prompt" hint for the member-less case.
>
> **Part B — samplers seed-and-show (build detail):**
> - **`identity.py`** — NEW `canonicalize_sampler_names(d)` mapping the file/llama.cpp namespace → our catalog namespace: **`temp→temperature`, `penalty_repeat→repeat_penalty`, `penalty_last_n→repeat_last_n`** (the ONLY divergences — top_p/top_k/min_p/typical_p/xtc_*/mirostat*/dry_* already match; verified vs `seed.py:324-368` catalog + `gguf.py:74` + `gguf_remote.py:84` `_GEN_CFG_TO_LLAMA`). Applied at the 3 samplers-producing sites (`derived_fields_from_meta:47` header + the two generation_config fallbacks `:67`/`:100`) so `model_samplers` (stored), the `/inspect` preview, AND the resolver all speak catalog names. **PANEL G-T2a (ship-stopper): without this, seeded `penalty_repeat`/`penalty_last_n` are silent no-ops at `/v1/ai/run` (`_plane2_extra` passes `flagName` VERBATIM, `prompts.py:321-327`) and show as unlabelled "Other keys" — the lifted-but-not-wired failure.** The GGUF PARSER (`gguf.py`) stays raw (its test `test_gguf.py:194` unchanged); normalization is the read→our-namespace boundary in `identity.py`. Requires a reseed (pre-prod; `model_samplers` only populate at download).
> - **Backend rename (PANEL R-T1 — plane-neutral):** `/model-catalog/switches`→`/model-catalog/resolved-defaults`; `ResolvedSwitchesResponse`→`ResolvedModelDefaultsResponse` (gains `samplers: list[ResolvedFlag]`); `ResolvedSwitch`→`ResolvedFlag`; fn `resolved_switches`→`resolved_defaults`; the endpoint reads the model's samplers from the `get_store().list()` row it ALREADY fetches for `mtpCapable` (one call, no 2nd endpoint). The injected `resolve_switches` callable param is UNCHANGED (it genuinely resolves only Plane-1 switches; samplers come from the store) → `install.py:137` untouched. Test `test_recommendations_catalog.py:179/186` endpoint path updated.
> - **`switchResolve.js`→`modelDefaults.js`** (PANEL R-T1): `resolveModelSwitches`→`resolveModelDefaults` returns `{switches, samplers, mtpCapable}`; drop the dead `fetchResolvedSwitches` (zero callers, grep-verified); update the 2 importers (`ConfigColumn.vue:31`, `TuneMeasureModal.vue:14`) + the `useRunnerModels.js:5` comment.
> - **`ConfigColumn.vue`** — ONE `seedFields(gridKey, srcKey, rows)` helper (the panel called it `maybeSeedGrid`; built as `seedFields`) (guard + conditional patch, no early-return — PANEL R-T3: NOT a copied guard) called for switches + samplers from the single `resolveModelDefaults` fetch; new `samplersSource` provenance ('model'|'preset'|'user') mirroring `switchesSource`; `patchPin` sets BOTH sources to 'model'; an `onEditSamplers` tagger + `writeOrder`/`writeStop` tag `samplersSource:'user'` (protect a custom order/stop from a model re-seed). **top_p (PANEL A-T1/§65 — model-filled, NOT task-owned):** seed the params-row `modelValue.topP` from the model's `top_p` when blank; ONLY `temperature` is dropped from the seed (task-owned, lives in the params row, excluded from the grid `:453`). The sampler grid gets the model samplers minus `temperature`+`top_p`.
> - **`CompareStrip.presetToConfig`** — also tag `samplersSource:'preset'` (a loaded preset's samplers are authoritative).
>
> **Why grid-level sampler seed == "task wins per-knob":** the sampler KnobGrid EXCLUDES `temperature`+`top_p` (`ConfigColumn:453`); temperature (the ONE task-owned sampler, §51/§65) lives in the params row — so the grid holds only the secondary knobs the task leaves blank. Seeding it (minus temp/top_p, with top_p routed to the params row) IS "the model fills the secondary knobs the task left blank." Mirrors the switch seed.
>
> **In-phase help (PANEL R-T11):** the "Send to Tasks Lab" flow + samplers seed-and-show land their JW help doc note in THIS phase (matching Phase-4's in-phase-help precedent), not deferred to Phase 6.
>
> **Final file touch-list:** Part A — NEW `ui/src/common/services/labHandoff.js`, `AiModelsArea.vue`, `TuneMeasureModal.vue`, `CompareStrip.vue`, `FeatureLab.vue`, `TaskKinds.vue`. Part B — `llm_runner/llm/identity.py` (+ `canonicalize_sampler_names`), `llm_runner/llm/model_catalog_api.py` (rename + samplers), `ui/src/switchResolve.js`→`ui/src/modelDefaults.js` (+ its importers + `useRunnerModels.js` comment), `ui/src/components/ConfigColumn.vue`, `ui/src/components/CompareStrip.vue` (shared w/ A). Tests: `tests/test_recommendations_catalog.py` (endpoint path), NEW coverage for `canonicalize_sampler_names` + the resolver's `samplers`. JW help doc note. (`LuModelCatalog.vue` from the terse list above is NOT needed — the tune modal is now `TuneMeasureModal.vue`.)
>
> **PANEL RECORD (3 checkers, pre-build, 2026-07-03):** Architecture-fit FAIL (A6 vanish-on-assign + wrong-guard-relaxed + top_p-dropped + the §65 mis-cite) — folded. Reuse/convergence FAIL (guard duplication → one `maybeSeedGrid`, built as `seedFields`; misleading `/switches` name → plane-neutral rename) — folded; confirmed `labHandoff` is genuinely new (no existing channel), B1 store-path sound. Grounding FAIL (sampler namespace mismatch ship-stopper + wrong-local-provider ship-stopper) — folded; confirmed `tuneRows`/`props.model.id`/the temperature-exclusion/`CatalogRow.samplers`-populated-at-download all check out. Non-blocking notes folded: help-in-phase, doc-path alignment, the module-global-tab persistence (benign).

## Phase 6 — Docs + final verification
- **This plan is persisted here** (`just-llm-runner/docs/plans/2026-07-02-gguf-grounded-model-layer.md`); keep the LIVE STATUS current as phases ship; note in both `MORNING_RECAP.md`.
- **Doc cleanups:** fix `model-catalog-research-and-recommendations.md` `:66` "4 jobs" vs `:71` 5-row matrix (5th = **Attribution, JV**); add the honest note that the routing picks (chat=small, the matrix) were reviewer-panel + user call, **not measured** (#28), grounded going forward by Tune & measure; cross-check the `:71` matrix against `2026-06-28-MASTER-PLAN.md` (both research docs are bannered "historical background").
- *(Code-adjacent docstrings — `identity.py`, `switch_resolve.py`, `install.py` comment — are updated in Phases 2/3, not here 【panel T11】.)*

## Verification (JW only — no JV)
- **Runner:** `ruff check llm_runner/ tests/` + `python -m pytest` (new: gguf range-read parse, identity mtp/ctx detect, switch_resolve no-auto-mtp + MoE+MTP-can-set-spec, grid resolver + tier bands + function map incl. `other`/`embed`).
- **Live pre-download probe (in a test):** `fetch_gguf_meta` on a real MTP repo → header range-read yields `nextn_predict_layers`>0 (mtp) + `expert_count` (type) with zero weight bytes; HF API yields `context_length`/size.
- **Renderer:** `npm run build:vite`; boot `python -m justwrite_server.cli serve --port 17495` + `npm run dev:vite`; `node scripts/headless-smoke.mjs` = **0 JS errors**.
- **Playwright probes** (reuse `findChrome()`): Edit form shows `type`/`mtp`/`ctx` as auto/read-only; add-from-link hydrates; the grid renders per-tier w/ live fit + quality/faster + download + `embed`/`other` columns; MTP-capable model shows `spec_type` default-off; **Tune → link → Tasks → a new Compare column appears seeded (incl. a custom switch)** even when tasks[0] has no members.
- **Per-phase rules-checker** on each diff; commit per phase; push. Docs (Phase 6) ride the series.

## Decisions (settled with the user this round)
1. Everything live from the file; nothing estimated. Keep `fit.py`; the file feeds it (#29).
2. File = source of truth; hand-editable = curation/policy only.
3. **MTP: detect (range-read), default OFF, measurable** — remove auto-apply + the orphaned mtp preset; `spec_type` opt-in from knob defaults.
4. **Grid = per-app functions (mapped from the 9 tasks, `other`+`embed` buckets) × seeded hardware tiers**; routing stays 9; chat = best-that-fits; quality/faster = display; JV adds attribution via `other` later.
5. ~~Additive: grid is the new discovery surface; catalog stays for load/unload/tune; flat-list removal deferred.~~ **SUPERSEDED 2026-07-03 by OPEN-A → UNIFY NOW:** the grid IS the single model surface; the flat catalog + the rank/why editor FOLD into one top-level **"Models" tab** (user-chosen placement); Providers → Built-in keeps only the engine-install panel + a pointer. The flat-list removal is DONE in Phase 4, not deferred. (Reuse-first: `LuModelCatalog`'s machinery is extracted/relocated, not rebuilt — `useRunnerModels` + `TuneMeasureModal`.)
6. Tune→Tasks handoff lands as a new Compare column under `tasks[0]` (robust to empty members), `labHandoff={providerId,model,switches}` tagged `'user'`.

## Out of scope (flagged)
Item 6 — QuickSetup `/v1/ai/jobs`→taskKind (#100) · JV **attribution** task+recs (JW grid extends via `other` later) · Decision 4 (switch-presets router removal) · **#28 measured benchmarks** (the grid *enables* measurement; it doesn't run the suite). *(The "full UI unification / removing the flat catalog" that used to sit here is now IN scope — Phase 4, per OPEN-A → unify-now.)*

## Panel record (for the resume)
3 independent rules-checkers (Opus) reviewed this plan pre-build; all folded:
- **Reuse/convergence:** T3 PASS (no forks/duplication — one parser local+remote, fit.py kept+fed, recommendations store reused, switchesSource/CompareStrip reused). Its FAIL was the T2 HF/MTP grounding (folded) + soft notes (embed data source, T4 second-option) — folded.
- **Architecture-fit:** FAIL T1 (coarse-map contradiction + undefined hardware bands), T1 (tasks[0] guarded mount + missing providerId), T3 (orphaned mtp preset), T11 (docstring timing) — all folded. NOTES: backend grid-resolver is correct (don't compute client-side); labHandoff singleton sound; "additive" coherent.
- **Grounding:** citations "unusually accurate"; one FAIL = the HF/MTP overclaim (folded, re-verified live: HF API `gguf` block = context_length/architecture/total only, no nextn/expert). Watch-items folded: use our own `qwen3.6-35b-a3b-mtp` as the MoE+MTP proof (not GLM, whose seed row has mtp unset); attribute "Metal net-loss" to web (#23752), not research `:89`; reframe `switch_resolve.py:53` as intentional-but-superseded, not an accident.
